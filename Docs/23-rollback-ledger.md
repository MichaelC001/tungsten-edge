# 23 · 回滚账本（Rollback Ledger）

> 这是**可执行的回滚账本**：目标 → 具体 `git revert` / `git merge` 命令 → 当前验证状态。
> 视觉版是 owner Obsidian 库 `进度看板.html` 的主线时间轴——看板上的标签是快速视觉状态，这张表才是执行依据；两者只需结论方向一致，不必逐字对应。
> 打检查点、或某个提交的可回滚性发生变化（新冲突、组合并）时，随检查点地图一起更新本表。

以下结论以 **2026-07-18 的 `a25add5`**（`finder-folder-preview` 最新功能检查点：消息应用纳入统一保留勾选模型）为起点；它与 `5f5efa0` 的 UserDefaults 数据边界已归档，入口见下方「数据边界」节。旧提交即使产品上是独立能力，也可能因为后来改过同一文件而无法直接 `git revert`；“人工”表示要解决冲突并重新验证，不表示功能不能移除。

## 当前本地主线（master）

以官方 `v0.6.5@9b4b5d0` 为起点的稳定重建线已于 2026-07-24 提升为本地 `master`；2026-07-25 归位后 `master` 直接检出在主目录 `/Users/caye/Projects/macos-dock-cc-v2`（原 `macos-dock-cc-v2-v065-stable` worktree 已拆除）。`codex/v065-stable-rebuild@3fb0df4` 冻结保留为 4.1 提升前检查点；旧本地 `master@d83b433` 保存在 `archive/master-before-stable-rebuild-20260724`；旧脏开发线 `codex/release-v0.6.6@5082100` 原样保留（其上封存的资产见下方专节）。

Owner 于 2026-07-25 决定本轮稳定重建停在 4.5；4.6 与后续一日回归暂停。4.5 稳定产品以发布提交 `dd85d86` 打包为正式 `v0.6.6`，标签固定在该提交。若未来恢复重建，只从本地 `master` 继续，不得误从旧开发线或远端 master 接续。

### 分支收拢（2026-07-25）

本地分支由 18 个收拢到 5 个，worktree 由 4 个收拢到 1 个（只剩主目录）。**删除前逐个用 `git merge-base --is-ancestor` 验证过提交仍可达，零丢失。**

保留的 5 个：

| 分支 | 用途 |
| --- | --- |
| `master` | 唯一主线，检出在主目录 |
| `codex/release-v0.6.6@5082100` | 旧脏开发线，**不得删除**——封存资产还在上面（见下方专节） |
| `codex/v065-stable-rebuild@3fb0df4` | 4.1 提升为 master 前的检查点 |
| `experiment/avoid-scale-hover@4b58365` | 避让方案对照试验 |
| `archive/master-before-stable-rebuild-20260724@d83b433` | 稳定重建前的旧本地 master |

删除的 13 个及其保活方式：

- 提交已全在 `master` 历史里（直接删）：`codex/menu-hotkey-adjustments-v066@b65d1ac`、`codex/release-v0.6.6-stable@552105b`、`codex/v065-4.5-process-liveness@b503fd6`、`codex/v065-4.4-messaging-admission@aad543d`、`codex/v065-4.3-quit-last@3bd1fef`、`codex/v065-4.2-open-gray@d226e80`、`codex/focus-return-trial@ed460c9`
- 是 `codex/release-v0.6.6` 的祖先：`finder-folder-preview@56f69ef`
- 已有发布标签指向尖端：`release/v0.6.0@7235191`（标签 `v0.6.0`）、`codex/release-v0.5.0@121724d`（标签 `v0.5.0`）
- 新建归档标签保活：`codex/contest-v0.5.0@f0b624e` → `archive/contest-v0.5.0-20260725`；`codex/finder-content-popover@297909d` → `archive/finder-content-popover-20260725`；`feat/focus-return@c030b93` → `archive/focus-return-20260725`

拆除的 worktree：`macos-dock-cc-v2-v065-stable`（master 已回主目录）、`macos-dock-cc-v2-release-publish-v0.5.0`、`macos-dock-cc-v2-release-v0.5.0`。

远端 `origin/master` 原停在 `121724d`（v0.5.0 时代），且不是本地 `master` 的祖先，仓库首页因此长期落后三个版本。2026-07-25 归位时已用 `git push --force-with-lease` 覆盖为本地 `master`。被覆盖的 3 个远端独有提交（`3f8f3dc` v0.4.5 发布说明、`4863d20` 合并、`121724d` v0.5.0 发布说明 + 版本号）实质无丢失：`v0.4.5` / `v0.5.0` 两个标签已推送且指向原提交，两份发布说明也已字节级补进 `Docs/Archive/Releases/`。Homebrew cask 引用 release 资产的 zip + sha256，不引用 master 分支，不受影响。

**只存在本地、未推送 GitHub 的标签共 6 个**（2026-07-29 实测 `git ls-remote --tags origin` 逐一确认）：`archive/contest-v0.5.0-20260725`、`archive/finder-content-popover-20260725`、`archive/focus-return-20260725`、`backup/finder-folder-preview-20260720`、`基础深色厚玻璃版`、`高透晶莹折射版`。不推送是**有意决定**（避免污染公开标签列表），代价是它们没有异地副本——**本机磁盘故障即永久丢失**。要消除这个单点，`git push origin <tag>` 逐个推即可；在此之前不要把它们当作可靠备份。

发布标签类型不统一：`v0.6.5` / `v0.7.0` 是轻量标签，`v0.6.6` 是附注标签。后果是 `git push --follow-tags` **只带附注标签**，v0.7.0 发布时因此静默漏推、需要单独 `git push origin v0.7.0` 才补上。**今后发布一律用附注标签 `git tag -a vX.Y.Z -m "..."`**；已发布的轻量标签不追改（会改写公开标签）。

2026-07-25 在正式发布标签 `v0.6.6@dd85d86` 上建立发布后功能分支 `codex/menu-hotkey-adjustments-v066`；`bcef0ed` 是已实机验收的「菜单与唤醒快捷键调整」检查点。它保留 v0.6.6 全部运行行为，只新增系统 Dock 设置入口、把钨极热键改为 ⌥⇧⌘D，并把「标记为消息应用」改为恒定标题 + 原生勾选态。该分支已于 2026-07-25 以合并提交 `7e0989e` 并入本地 `master`（无冲突，账本两侧条目均保留），分支本身随本轮分支收拢删除；后续不得从封存旧开发线重放这三项功能。

2026-07-29 发布 `v0.7.0@d4f1942`（标签固定在该提交），内容 = v0.6.6 之后的菜单/快捷键调整 + 本轮三块：系统 Dock 彻底隐藏（`d08e8d6`）、扫描准入修复（`42f70eb`）、README 微信群二维码（`203b69c`）。发布提交与 v0.6.6 一样直接打在 `master` 上；发布当时 `master` 未再 bump，因此**发布后的开发构建与已发布包一度同为 `0.7.0 (8)`，靠版本号无法区分**（`cfd6a71` 已把主线构建号推到 `10`，暂时不再撞号；下次发布后会重新撞上，别指望版本号）——这一点在 v0.6.6 已经踩过一次（用户报「菜单里没有『打开系统 Dock 设置…』」，实为发布落后于开发线 14 小时），排查同类问题时先比对提交而非版本号。Homebrew cask 已同步至 `0.7.0`（`moonbai-studio/homebrew-tungsten-edge@d9f9b88`）。

2026-07-30 发布 `v0.7.1@baa2f9f`（**附注标签**，按上文新规矩；`v0.6.5`/`v0.7.0` 那两个轻量标签不追改）。内容 = v0.7.0 之后的「菜单四件事」三批：系统 Dock 滑杆回归 + ⌥⌘D 语义还原（`8e6453e`）、显示中转站开关（`2c0042f`）、任务条四档缩放（`698a266`）。发布提交同样直接打在 `master` 上，版本号 `0.7.1 (13)`。**本版有一条对用户可见的行为反转**：`d08e8d6` 的「彻底隐藏」被撤销，菜单命令回到普通自动隐藏（等价 ⌥⌘D），彻底隐藏改由新滑杆右端「不唤醒」承担——发布说明把这条单独放在最前面。Homebrew cask 已同步至 `0.7.1`（`moonbai-studio/homebrew-tungsten-edge@48f6c2d`）。发布后 `master` 已前进到启动崩溃修复 `328bc59`，但未再 bump，**开发构建与已发布包仍同为 `0.7.1 (13)`**，排查问题先比对提交。

2026-07-31 发布 `v0.7.2@fbdac51`（**附注标签**）。内容 = v0.7.1 之后三项：启动期布局循环崩溃修复（`328bc59`，四档各 25 次共 100 次冷启动 0 崩溃）、空闲 CPU 中位数 2.60% → 1.40%（`683f1fc`）、面板承载器收尾（`b2b6a38`）。发布提交同样直接打在 `master`，版本号 `0.7.2 (14)`。**本版无用户可见的行为反转**，两项都是修复/优化，交互与设置一律不变。Homebrew cask 已同步至 `0.7.2`（`moonbai-studio/homebrew-tungsten-edge@ece5faf`）。发布后 `master` 未再 bump，**开发构建与已发布包同为 `0.7.2 (14)`**，排查问题先比对提交。

2026-08-01 发布 `v0.7.3@418233b`（**附注标签**）。内容 = v0.7.2 之后两项：启动弹跳提前停在最高点（`a645590`，动画作用域 + 启动会话双修）、剪映点不开 + 系统设置弹跳 20 秒（`6414f9d`，启动目标包解析 + 空标题标准窗口准入）。发布提交同样直接打在 `master`，版本号 `0.7.3 (15)`。**本版无用户可见的行为反转**，三项都是修复，交互与设置一律不变。Homebrew cask 已同步至 `0.7.3`（`moonbai-studio/homebrew-tungsten-edge@0ad7cac`）。发布后 `master` 未再 bump，**开发构建与已发布包同为 `0.7.3 (15)`**，排查问题先比对提交。发版前全表复检结果见上方 2026-08-01 那条（46 条 / 6 条仍可单点撤销 / 唯一新增衰减 `a645590`）。

2026-08-05 发布 `v0.7.6@693bab8`（**附注标签**）。内容 = v0.7.5 之后三项，全部来自用户 Phoebus 的反馈：跨机器重启保持普通 kept 应用的图标次序 + 运行态 POSIX 兜底对账（`7bd9910`）、多进程应用重复兜底卡（`84157c9`，WPS 两个窗口显示三张卡）、窗口收编逐进程诊断（`6bbe3fe`，默认关）。发布提交直接打在 `master`，版本号 `0.7.6 (18)`。**本版无用户可见的行为反转**——三项都是修复，没有拿掉任何用户选过的东西；但「重启后保留应用的图标顺序会保持」是**用户能直接看出来的新行为**，且版本号是 patch 不会暗示有变动，因此仍按 runbook 放在发布说明最前面，并写明「窗口卡片的顺序依旧不跨重启保留」这个刻意的边界。Homebrew cask 已同步至 `0.7.6`（`moonbai-studio/homebrew-tungsten-edge@04a41d4`）。发布后 `master` 未再 bump，**开发构建与已发布包同为 `0.7.6 (18)`**，排查问题先比对提交。发版前全表复检见下方 2026-08-05 那条（表内 99 条 / 7 条仍可单点撤销 / 唯一新增衰减 `8af961c`）。**本轮唯一未解决的用户反馈**：锁屏恢复后消息应用的聊天窗跑回窗口区最左，诊断已随包发出（默认关），等复现。

2026-08-04 发布 `v0.7.5@ce7b4cd`（**附注标签**）。内容 = v0.7.4 之后两项：一页式设置窗口 + 右键任务条弹菜单 + 修掉空白「设置」死入口（`f3b43a1`）、菜单粘滞修复 + 设置面系统读取移出主线程（`aad9ce3`）。发布提交直接打在 `master`，版本号 `0.7.5 (17)`。⚠️ **本版有一条用户可见的行为反转**：菜单里可点的「显示/隐藏 Tungsten Edge 钨极」命令删除，外观四项（显示中转站 / 鼠标悬停显示应用名 / 最大化窗口避开任务条 / 任务条大小）搬进新的设置窗口；**版本号是 patch，不会向用户暗示行为变更**，因此放在发布说明最前面。Homebrew cask 已同步至 `0.7.5`（`moonbai-studio/homebrew-tungsten-edge@810ea70`）。发布后 `master` 未再 bump，**开发构建与已发布包同为 `0.7.5 (17)`**，排查问题先比对提交。发版前全表复检见上方 2026-08-04 那条（24 条 / 2 条仍可单点撤销 / 零意外衰减）。

⚠️ **本次发版是接力完成的，两处教训值得记**（2026-08-04）：（一）打包脚本**中途断在 `hdiutil` 那一步**，`dist/` 里只有 zip、没有 DMG，而 zip 看上去是好的——半成品的 `dist/` 是错哈希最可能的来源。收尾时**重跑了整个 `package_release.sh`**（它开头就 `rm -rf dist`），让两个产物出自同一次完整运行，别在半成品上续。（二）发布说明初稿写着「或按 ⌘, 打开设置窗口」，而 **⌘, 在正常运行时根本打不开钨极的设置**（`.accessory` 应用没有菜单栏，快捷键无处挂载，只在权限引导态管用；`AGENTS.md` 有这条护栏）。这条错误发出去会让用户按了没反应、再次得出「根本设置不了」——正是本版要解决的那个抱怨。已在 `089cc92` 订正为「菜单栏图标 / 右键任务条」两条真实入口。**runbook 里「写完先让 owner 扫一眼再发」那道闸就是为这个存在的，不要跳。**

2026-08-03 发布 `v0.7.4@df3cd63`（**附注标签**）。内容 = v0.7.3 之后七项：消息区图标档位系数（`5f59c72`）、系统 Dock 菜单组重做（`a6a31f7`）、交接目标不收无标题窗口（`97dacd5`）、拿掉图标状态淡化（`0482a77`）、悬停两档设置（`400bc9c`）及其改名（`7b61767`）、最大化避让做成默认关的开关（`62e212e`）。发布提交同样直接打在 `master`，版本号 `0.7.4 (16)`。⚠️ **本版有两条用户可见的行为反转**：避让从恒开改为默认关（老用户升级后避让消失，需自行勾选恢复）、系统 Dock 组删掉显隐命令改为滑杆+确认（一键显隐交还 macOS 的 ⌥⌘D）；**版本号是 patch，不会向用户暗示行为变更**，因此两条都放在发布说明最前面。Homebrew cask 已同步至 `0.7.4`（`moonbai-studio/homebrew-tungsten-edge@d9d69b1`）。发布后 `master` 未再 bump，**开发构建与已发布包同为 `0.7.4 (16)`**，排查问题先比对提交。发版前全表复检见上方 2026-08-03 那条（21 条 / 2 条仍可单点撤销 / 零新增衰减）。**本次发版流程已落成仓库内 runbook：`Docs/29-release-runbook.md`**，此前它只存在于某个 agent 的私有记忆里、别的 agent 读不到。

安装回退：若需恢复上一稳定安装版 4.4，使用备份 `/Users/caye/Projects/tungsten-edge-rebuild-artifacts/2026-07-25-stage4/4.5-process-liveness/rollback/Tungsten Edge.app`；若需恢复 4.3，使用备份 `/Users/caye/Projects/tungsten-edge-rebuild-artifacts/2026-07-24-stage4/4.4-messaging-admission/rollback/Tungsten Edge.app`；若需恢复 4.2，使用备份 `/Users/caye/Projects/tungsten-edge-rebuild-artifacts/2026-07-24-stage4/4.3-quit-last/rollback/Tungsten Edge.app`；若需恢复 4.1，使用备份 `/Users/caye/Projects/tungsten-edge-rebuild-artifacts/2026-07-24-stage4/4.2-open-gray/rollback/Tungsten Edge.app`；若需恢复到 v0.6.5 原始安装，使用官方备份 `/Users/caye/Projects/tungsten-edge-rebuild-artifacts/2026-07-23-stage4/4.1-f2-first-frame-position/rollback/official-v065-20260723-221811/Tungsten Edge.app`（executable 名均为 `macos-dock-cc-v2`）。

- GitHub `v0.7.6` DMG hash: `97f6a47336137f65c29d80c7aca3aab68613a640f2d96f0f33339f5f7b88c48a`
- GitHub `v0.7.6` ZIP hash: `86fd1e96e7c5a1308b47733f4ecd96ad5d023baa837aeceeb6c7592e2e3a0f33`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@04a41d4` 引用此值；**推 cask 之前**已从 GitHub 下回已发布的 zip 实测比对，cask / 已发布包 / 本地产物三方一致）
- GitHub `v0.7.5` DMG hash: `f93892cbd8f7de2e22253d0b114e43e0957d606e90e1fcc58ff20cafd3a7f489`
- GitHub `v0.7.5` ZIP hash: `456893e18c3118ca26e7c81ee875227d386123546a373e52bb0a42e677ffc6d3`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@810ea70` 引用此值；**推 cask 之前**已从 GitHub 下回已发布的 zip 实测比对，cask / 已发布包 / 本地产物三方一致）
- GitHub `v0.7.4` DMG hash: `ba3261f6e4705a292b242bad5164f2c283ab12e1bc498baf9ace9b3496a9b31e`
- GitHub `v0.7.4` ZIP hash: `33dff366dec70b7677222847999d542b045029848536d4d52e590dcb0c1026a2`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@d9d69b1` 引用此值；**推 cask 之前**已从 GitHub 下回已发布的 zip 实测比对，三方一致——这个顺序比事后校验安全，不一致的哈希根本到不了用户）
- GitHub `v0.7.3` DMG hash: `747dd0e1c260666c7ed3f0215254e3edf19825e33944b71fe33ebec0a0bca7db`
- GitHub `v0.7.3` ZIP hash: `2d81c99ecb30eb91dff6c14c10f1c495ee4c194cde11212d5c4aca5744f082e5`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@0ad7cac` 引用此值；两个哈希均已从 GitHub 重新下载实测比对一致）
- GitHub `v0.7.2` DMG hash: `6a090a8dcc0bfd63399bc405ea45a190041a1a8864273624a904569335f023b9`
- GitHub `v0.7.2` ZIP hash: `77d792ef053b343928a7ae3a653265432b439a1c15d063868cc12ca3a881689f`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@ece5faf` 引用此值；两个哈希均已从 GitHub 重新下载实测比对一致）
- GitHub `v0.7.1` DMG hash: `3835c0df16c28fdeb0ddf8444374c2374a1c8744e61d7cfc5ff584822d328f84`
- GitHub `v0.7.1` ZIP hash: `4ea6a75f5a2f59e89b92cd689bd848da23d96c41c4b6925bd7456bbbfc88c5d9`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@48f6c2d` 引用此值；两个哈希均已从 GitHub 重新下载实测比对一致）
- 当前稳定安装版（4.5）hash: `db4a18e2d4d8bdd4db13fab44e5fee101d1eb0aff6642d66945d250c68417a11`
- GitHub `v0.7.0` DMG hash: `65e33403a882445815f2215a74dc576bd0f2bec700eeadf5a9c014cb03b42bc8`
- GitHub `v0.7.0` ZIP hash: `62391c2a05456af1558c870eb0293bcd6c6319209347d37b87dcf49ead22add3`（Homebrew cask `moonbai-studio/homebrew-tungsten-edge@d9f9b88` 引用此值）
- GitHub `v0.6.6` DMG hash: `10a53b8903f5b19238272f6ad154b449b962a224ba47db63aae51f7fda65c04e`
- GitHub `v0.6.6` ZIP hash: `3ebe3f6758ec8804e78503e5b441b3a071162b83f421b3727132a40eb1475bad`
- 公开包边界：ad-hoc 签名、未公证；`v0.7.2` 版本号 `0.7.2 (14)`、`v0.7.1` 版本号 `0.7.1 (13)`、`v0.7.0` 为 `0.7.0 (8)`、`v0.6.6` 为 `0.6.6 (7)`，均 arm64 + x86_64
- 上一稳定安装版（4.4）备份 hash: `9b1bc1d19ca3c1ccbaade0fdf2b80a64dd0a0f59dd2805ca8279aa47e91befb6`
- 4.3 备份 hash: `ed98039e4139d2e50ff3f4e7cf6938d5ad2ef1586fb5b82e5494a76bedd4a5ca`
- 4.2 备份 hash: `6defba5dcb6313ede6961e5e591061fb48850def0005478bcde1bbd95ce93986`
- 4.1 备份 hash: `0b92d6d2f90fb3602bbc010987760971df89444cb7c459cf1f4a0f37c5756334`
- 官方 v0.6.5 备份 hash: `a9da38bf2f98f7ebe432d83e5cb9ac798d58297bc93b1422b03a69053e3ebb3f`

## 封存在废弃开发线上的资产（尚未搬回稳定线）

回退到 v0.6.5 重建时，旧脏开发线 `codex/release-v0.6.6@5082100` 上有一批**与聚焦缺陷无关**的发布/权限基建没有跟着搬回来。Owner 于 2026-07-25 决定本轮暂不搬，只在此登记，避免以后想不起来还有这回事。要用时按下表 `git cherry-pick` 或 `git checkout <commit> -- <path>` 取回，取回后必须重新验证。

| 资产 | 取回位置 | 对当前稳定线的影响 |
| --- | --- | --- |
| Developer ID 签名 + 公证打包链（`Scripts/package_release.sh`，相对稳定线 +275 行，fail-closed：凭据或验证缺失即停，不回退临时签名） | `codex/release-v0.6.6`，提交 `2c2a51a` | 稳定线的 `Scripts/package_release.sh` 仍是 v0.1.0 时代的 ad-hoc `codesign --force --deep --sign -`。**已发布的 `v0.6.6` 公开包因此未签名未公证**，用户首次打开需右键「打开」或在「隐私与安全性」里「仍要打开」（发布说明已写明） |
| ~~辅助功能权限重授权引导~~（提交 `26371b8`） | **已出表：2026-07-30 在主线重建，见 `ec930ae`** | **不要再取回 `26371b8`。** 主线的是按 owner 2026-07-25 约束重新实现的，**不是**它的副本：`.stalled` 不再被当作「旧条目已失效」的判定、位置判据从 `/Volumes` 路径前缀改成卷只读、并且新增了旧实现完全没有的运行期失效检测与自动恢复。`26371b8` 只剩参考价值（`git show` 可读），照搬会退回被否决的方案 |
| 自签试用包脚本 `Scripts/package_preview.sh`（239 行） | `codex/release-v0.6.6`，提交 `7943618`（同 `v0.6.6-beta.1` 标签） | 稳定线没有试用包打包流程 |
| `Resources/TungstenEdge.entitlements` | `codex/release-v0.6.6`，提交 `2c2a51a` | 公证链依赖；稳定线 `Resources/` 下只有 `Assets.xcassets` 与 `Info.plist` |
| 最小化时的背景窗口焦点交接（`Platform/Accessibility/BackgroundFocusHandoff.swift` 116 行 + `AccessibilitySource.swift` 相应改动） | `codex/release-v0.6.6`，提交 `df482d3`「最小化命令提交与状态确认分离 + 背景窗口精确交接」 | 稳定线的聚焦 / 最小化路径**与官方 `v0.6.5` 完全相同**——`git diff --name-only v0.6.5 master` 在 `Platform/Accessibility/`、`Core/Lifecycle/ActionPlanning/`、`TabFoldDecision.swift` 上零改动，整条重建线从未碰过这里。废弃线相对稳定线在该路径上共 +383/−82 行。**注意：owner 2026-07-26 已明确排除 `df482d3` 打包出的 `v0.6.6-beta.2`（「试过肯定不对」）**；废弃线在 beta.2 之后还有一版 carbon-before 交接方案，只存在于已丢失的脏工作区，不可取回。取回本条前必须先与 owner 确认要的是哪一版行为 |

上述各项的提交均已验证可从 `codex/release-v0.6.6` 到达，该分支因此**不得删除**。同线上的 `finder-folder-preview` 是它的祖先，删除后不影响这些资产。权限引导那条虽已出表，该分支仍不得删除——理由见下方两条。

**该分支还有第二个不得删除的理由**（2026-07-29 审计补记）：Obsidian 地图与本账本引用的两条**回摘源提交** `0b74fa6`（启停首帧影子滑动，回摘为 master 的 `8af961c`）和 `5e2e82e`（消息区准入，回摘为 master 的 `aad543d`）**只靠这个分支保活**——实测它们不在 `master` 上，也没有任何标签指向。只按「四项封存资产」的理由判断而删掉该分支，会连带让这两条源提交变成悬空对象、被 `git gc` 回收，届时地图上的引用将无法追溯。

## 第二条封存线：`parked/fullscreen-edge-wake`（2026-08-05）

与上面那条废弃开发线无关，是一次**做完了但主动不上线**的功能，单独开线封存。**该分支不得删除。**

| 项 | 内容 |
| --- | --- |
| 分支 / 提交 | `parked/fullscreen-edge-wake` @ `eac46e4` |
| 内容 | 全屏下从底边唤出任务条。`PanelVisibilityState` 把 `.fullscreen` 拆成模式标志 `isFullscreen` + 独立隐藏原因 `.fullscreenConcealment`，新增纯判定 `FullscreenWakePolicy` / `FullscreenProbeDecision`；`PanelCoordinator` 引入 `effectiveEdgeDelay` 派生属性（替换 8 处）、全屏探针代次隔离、避让全屏门禁、冷启动与跨屏补探测、`DOCK_FULLSCREEN_WAKE` kill switch |
| 验证状态 | Debug 构建通过、694 单测全绿（含 9 个新增全屏用例，逐个确认执行）。**未实机验收**——因 Dock 冲突无解，验收无从做起 |
| 为什么不合入 | 全屏下贴底边时系统 Dock 被同一手势唤出并盖在任务条上（Dock 在窗口层级 20，本应用面板在 `.floating`/3），四条消除路径逐条实测走死。不选「合入但默认关」：这是任务条**核心显隐状态机**的重写，默认关等于让所有用户承担重构风险却无收益 |
| 对当前稳定线的影响 | 零。master 的全屏行为与 v0.7.6 完全相同（`git diff v0.7.6..master` 在 `App/`、`Core/`、`Platform/`、`UI/` 上零改动） |
| 取回方式 | 整体 `git merge` / `git cherry-pick eac46e4`；取回后必须先处理提交信息里列的 6 处待处理项，并重新实机验收 |

**分支里另有三处与全屏唤出无关的现存 bug 修复，值得单独 cherry-pick**（`git checkout eac46e4 -- <path>` 取不了，改动与重构交织，需手工挑）：① 冷启动时用户已在全屏空间——主线完全不探测，任务条会盖在全屏画面上；② 任务条跨屏切到有全屏窗口的另一块屏——主线不重新判定，会浮在该应用画面上；③ 过期的异步 AX 全屏探针结果可以乱改可见性（主线无代次隔离）。

技术实测细节见 `Docs/05-known-platform-quirks.md`《全屏下的系统 Dock：四条配置路都阻止不了底边唤出》，产品决定与再开条件见 `Docs/27-product-decisions.md`。

**2026-08-06 补记**：本条封存的前提正在被另一条线挑战——`codex/fullscreen-edge-filter-spike`（worktree 在 `../macos-dock-cc-v2-fullscreen-spike`，从 `eac46e4` 往上接着做）在试第五条路：用 `CGEventTap` 把光标从最底边夹开，让 Dock 的边缘触发根本不发生。若该方案成立，本条封存与 `Docs/27` 的暂缓决定都要重新评估。**在它有结论之前，不要按「已定不做」处置本分支。**

## 数据边界：UserDefaults 键迁移

代码 revert 不会回退用户磁盘上的数据。`5f5efa0`（抽屉位置与保留拆分）和 `a25add5`（消息应用纳入统一保留勾选）两次改动的键矩阵与回滚后行为，见 `Docs/Archive/Engineering/25-userdefaults-data-boundaries.md`；键的当前权威定义在 `AGENTS.md` 的 Kept Apps 一节。

**`stripKeptAppOrderV1`（2026-08-04 新增，归属 `StripOrderStore`）**：普通 kept 应用的跨重启 bundleID 排名，**只存 bundleID、不存任何窗口 id**。写入时机 = 每次 `sync` / 拖动重排后由当前 chip 顺序反推；成员集合跟随 `KeptAppStore − MessagingAppStore` 增删。首次迁移从旧 `stripLiveOrder` 存档里可识别的 kept `app-*` 取序，再把 `KeptAppStore` 剩余顺序追加在后。**回退行为**：该键会成为孤儿键（无害，不再被读），跨重启的应用级次序退回「按当初勾选『在程序坞中保留』的先后」——即用户拖出来的顺序在重启后丢失，这正是它要修的症状。同开机周期内的精确 chip 序仍由 `stripLiveOrder` + `stripLiveOrderBootTime` 负责，不受影响。

## 回退命令有有效期（2026-07-29 建立）

下表每条的「当前实证」都是**登记当时**在那棵树上验证的，**不是永久保证**。后续提交碰到同一批文件后，`git revert` 会从单点变成需要手工解冲突。读这张表时必须先看下面的复检结果，不要直接把「已验证 revert」当成现在还成立。

**复检方法**（只读，不改动工作区，不需要建 worktree）：

```bash
git merge-tree --write-tree --merge-base=<提交> HEAD <提交>^
# 退出码 0 = 仍可干净回退；非 0 = 有冲突，输出里列出冲突文件
```

注意别用 `git show <提交> | git apply --check --reverse -` 代替：`git apply` 要求上下文严格对齐，而 `git revert` 走三方合并，前者会给出大量假阳性（2026-07-29 首次复检就被误导过一次）。二进制文件在 `git apply` 下也必然假阳性。

**最近一次全表复检：2026-08-05（v0.7.6 发版前，HEAD=`38aba0f`，表内出现的提交号 99 条逐条实跑 `git merge-tree --write-tree --merge-base=<提交> HEAD <提交>^`）**——仍可单点撤销 7 条：`683f1fc`、`6bbe3fe`、`84157c9`、`aad9ce3`、`b2b6a38`、`c9cb5a4`、`edf9b64`；**唯一新增衰减是 `8af961c`**（启停首帧影子滑动），被本轮的跨重启 kept 排名改动压死——两者同改 `App/Composition/StripOrderStore.swift` 与 `AGENTS.md`，属预期而非意外。⚠️ **口径提醒：本轮的 99 条与历史那几行的 24 / 21 / 48 不可直接相减**——历史几轮数的是主表的行数，本轮数的是「表格行里出现过的全部提交号」（含各行「当前实证」正文里引用的提交），是个超集。所以本轮多出来的 `683f1fc` / `b2b6a38` / `c9cb5a4` / `edf9b64` 不是「起死回生」，是上几轮压根没纳入统计。**以后要跟本轮比，就沿用本轮口径**。另：`git merge-tree` 带不带 `--write-tree` 实测结果一致，runbook 里那段命令没问题。

**上一次全表复检：2026-08-04（v0.7.5 发版前，HEAD=`bb8ff79`，表内 24 条逐条实跑 `git merge-tree --merge-base=<提交> HEAD <提交>^`）**——仍可单点撤销的只有 `aad9ce3` 与 `8af961c` 两条（v0.7.4 时还干净的 `62e212e` 已掉队：被 `f3b43a1`/`aad9ce3` 这两个同改 `StatusMenuController.swift` + `AppDelegate.swift` 的提交压死，它自己登记时就预告过这一点）；`f3b43a1` 已衰减（被 `aad9ce3` 压死，下方该条已登记）；其余条目的 ⚠️ 说明依旧成立（`bcef0ed` 是 ⛔ 退役，不参与）。本轮没有出现「记录说 ✅、实跑是冲突」的意外行——`62e212e` 的衰减发生在 v0.7.4 发版之后的两个功能提交里，属预期。

**上一次全表复检：2026-08-03（v0.7.4 发版前，表内 21 条逐条实跑 `git merge-tree`，零新增衰减）**——结果与表内已记状态**逐条一致**：仍可单点撤销的只有 `62e212e` 与 `8af961c` 两条，其余 19 条的 ⚠️ 说明依旧成立（`bcef0ed` 是 ⛔ 退役，不参与）。本轮没有出现「记录说 ✅、实跑是冲突」的行。同期把 `62e212e` / `0482a77` / `97dacd5` 三条的验收状态从「待完成」改为 owner 实机验收通过。

**上一次全表复检：2026-08-02（`a6a31f7` 系统 Dock 菜单组重做后，48 条逐条实跑 + 对照跑，零新增衰减；详见下方表格前的复检记录）**

**最近一次抽检：2026-08-03（`62e212e` 避让开关后，跑了当时还挂 ✅ 的 2 条）**——结果：**`7b61767` 被本轮压死**（对照跑 `--merge-base=7b61767 62e212e^ 7b61767^` 退出码 0，确认提交前还干净；唯一真冲突落在 `StatusMenuController.swift`，`Docs/27` 与 `AppSettingsStoreTests.swift` 都能自动合）、`8af961c` 仍干净。**这是本表第七个「刚登记就被下一个提交打掉」的实例**，而且和第六个是同一个死因、同一批文件——避让开关这次加的正是「菜单加一个勾选项 + 设置项 + 单测」，跟改名那条改的是同几处。⚠️ 这次同样没跑 ⚠️ 那些行。

**上一次抽检：2026-08-02（`7b61767` 悬停设置改名为勾选项后，跑了当时还挂 ✅ 的 3 条）**——结果：**`400bc9c` 被本轮压死**（对照跑 `--merge-base=400bc9c 7b61767^ 400bc9c^` 退出码 0，确认提交前还干净；冲突落在 `AppSettingsStore.swift` / `StatusMenuController.swift` / `Docs/27` / `AppSettingsStoreTests.swift` 四个文件——改名动的正是它建的那几处）、**`97dacd5` 也已衰减，但凶手不是本轮**（见下方新增的那段教训）、`8af961c` 仍干净。这是本表第六个「刚登记就被下一个提交打掉」的实例。⚠️ 这次同样没跑 ⚠️ 那些行。

**上一次抽检：2026-08-02（`400bc9c` 悬停效果两档后，跑了当时还挂 ✅ 的 3 条）**——结果：**`0482a77` 被本轮压死**（对照跑 `--merge-base=0482a77 400bc9c^ 0482a77^` 退出码 0，确认提交前还干净；冲突落在 `DockStripView.swift` / `LauncherChip.swift` / `Docs/27`）、`97dacd5` 与 `8af961c` 仍干净。**这是本表第五个「刚登记就被下一个提交打掉」的实例**，而且 `0482a77` 自己那条登记时就写着「碰了 `AGENTS.md` 和 `Docs/27`，下一个改它们的提交就会让它衰减」——**预言当天应验**。⚠️ 这次同样没跑 ⚠️ 那些行。

**上一次抽检：2026-08-02（`0482a77` 拿掉图标淡化后，只跑了当时还挂 ✅ 的 4 条）**——结果：`385f228` 被本轮压死（对照跑确认提交前还干净）、`203b69c` 早就被 `edf9b64` 压死（**不是本轮，是上次全表复检漏掉的**）、`97dacd5` 与 `8af961c` 仍干净。**⚠️ 这次没跑 ⚠️ 那些行**，它们的说明可能同样过时。

> 同日现场教训（**一天之内验证了两次**）：`f1efa7c` 那次复检把自己登记成「✅ 单点成立」，**两个提交之后就不成立了**——`ed31ff2` 同时碰了 `DockStripView.swift`、`AGENTS.md` 和 `project.pbxproj`。这不是意外，是本表下方列的两个高频接触面加上功能文件本身，一次全踩。
>
> 紧接着 `c6dfdb7` / `b7abf7e` 登记的「✅ 单点成立」也是**下一个提交就死**：`121aebf`（图标淡化转正）bump 了构建号 → 压中 `c6dfdb7` 的 `project.pbxproj`；在《Light / Dark Appearance》加了一条护栏 → 压中 `b7abf7e` 的 `AGENTS.md`。一个提交、两个高频文件、两条刚登记的条目全废，**且这次是对照跑确认的（`git merge-tree ... 121aebf^ <提交>^` 显示两条在提交前都还干净），不是猜的**。
>
> 所以「刚登记的条目」和老条目一样不可信，读表永远先看日期和这一段。
>
> **2026-08-02 新增的第三种死法：条目被「登记它的那次提交」自己打掉。** 上一轮抽检把 `97dacd5` 记成「仍干净」——**那一刻是真的**，但紧接着写这条记录的 `adfb4a1`（账本登记 + 补两条踩坑）碰了 `Docs/28-process-pitfalls.md`，而 `97dacd5` 恰好也带着 `Docs/28` 的改动，于是**记录墨迹未干就作废了**。对照跑坐实：`97dacd5` 对 `400bc9c` 退出码 0、对 `adfb4a1` 退出码 1。
>
> → **复检必须跑在登记提交之后，不能跑在之前。** 账本提交自己就碰 `Docs/23`（永远）和 `Docs/28`（经常），所以它天然会打掉任何带这两个文件的条目。可行做法：先写先提交，然后再跑一遍复检、发现变化就补一次修正提交（本轮就是这么发现的）；或者至少在登记时写明「本次登记提交自身碰了哪几个文件」。

**浅色这一轮（`f1efa7c` … `385f228` 共 11 个提交）互相层叠得很厚**，逐个单独回退基本都不成立；要整轮撤销就按提交倒序连着 revert，别挑单个。其中**「图标淡化」那一支已经被 `0482a77` 整个撤销了**（owner 2026-08-02 决定不做这个区分），别再去回退 `121aebf` / `385f228` 想恢复它——先 revert `0482a77`。

| 条目 | 是否仍可干净回退 | 说明 |
| --- | --- | --- |
| `aad9ce3` 菜单粘滞修复 + 设置面系统读取移出主线程 | ✅ 单点成立（最新提交，按上面那段默认它随时会废；**它自己带了 `Docs/23` 之外的两个高频接触面 `AGENTS.md` / `Docs/27` / `Docs/28`，下一个碰其中任意一个的提交就会让它衰减**） | 2026-08-04 新增，登记当时 `git merge-tree` 退出码 0（`--merge-base=aad9ce3 HEAD aad9ce3^`）。**里面是两件互不依赖的事，回退代价差别很大，尽量别整条撤**：（一）**菜单粘滞修复**只在 `PanelCoordinator.swift` 加了 61 行（菜单跟踪期间摘掉全局鼠标监视器），回退后菜单里鼠标划过选项重新粘滞——这是 owner 亲自报、亲自验收的手感问题，**不要回退这一半**；（二）**设置面异步化**碰 11 个文件，回退后打开菜单重新有约 100ms 主线程阻塞（登录项 XPC + 系统 Dock 偏好 + 辅助功能权限同步读），右键菜单锚点最多偏 42pt 的老 bug 回来，菜单在屏期间可能增删行导致高度跳的老毛病也回来。**不涉及任何数据迁移**：没有新增 UserDefaults 键；`LaunchAtLoginServicing` / `NativeDockPreferencesServicing` 两个协议方法改成 `async` 是纯进程内契约。碰面：`PanelCoordinator.swift` / `StatusMenuController.swift` / `AppDelegate.swift` / `SettingsCoordinator.swift` / `LaunchAtLoginService.swift` / `NativeDockPreferencesService.swift` / `SettingsWindowController.swift` / `SettingsWindowView.swift` / `PermissionRecoveryCoordinator.swift` + 三个测试文件 + `AGENTS.md` + `Docs/27` + `Docs/28`。**验证状态：660/660 单测全过（净 +4，全部是并发顺序用例）；Release 固定证书装机后 `sample` 实测——菜单开着的 2724 个采样里主线程 93.2% 阻塞在 `mach_msg2_trap` 等事件、钨极自己的代码 0 个采样，`SMAppService` 只出现在 `launch-at-login-reader` 后台队列上；owner 实机验收通过（2026-08-04，原话「现在好了，实测很顺畅了」）**。⚠️ 无「修复前 + 菜单开着」的对照采样，所以能证明的是"修完主线程干净"，给不出前后差值 |
| `f3b43a1` 一页式设置窗口 + 右键任务条弹菜单 + 修掉空白「设置」死入口 | ⚠️ **已衰减**（`AGENTS.md` + `Docs/27` + `Docs/28` + `StatusMenuController.swift` + `AppDelegate.swift` 等）——2026-08-04 被 `aad9ce3` 压死，**上一轮登记时预告的自伤没等到登记提交自己，先被下一个功能提交打掉了**；对照跑坐实：登记当时退出码 0，现在退出码 1 | 2026-08-04 新增，登记当时 `git merge-tree` 退出码 0（`--merge-base=f3b43a1 HEAD f3b43a1^`）。**回退代价很大，不是一条能随手撤的功能**：设置窗口整个消失，⌘, 会**退回到那个空白窗口**（`Settings { EmptyView() }` 占位场景仍在，`CommandGroup(replacing: .appSettings)` 一撤那条自动生成的菜单项就回来），也就是回退把用户报的原始 bug 一并放回去；右键任务条弹菜单、菜单分组与两顶灰帽子、「设置…」与「打开系统 Dock 设置…」的底部分组一并消失，可点的「显示/隐藏 Tungsten Edge 钨极」命令**恢复**，菜单栏应用名退回 `macos-dock-cc-v2`。**不涉及任何数据迁移**：没有新增 UserDefaults 键，设置窗口读写的全是既有键；`CFBundleName` 改动不影响 TCC 授权与登录项（它们按 bundle id + 签名匹配），回退不会让用户重新授权。碰面极广且全是高频接触面：`StatusMenuController.swift`（重写近半）/ `AppDelegate.swift` / `DockStripView.swift` / `PanelCoordinator.swift` / `AppMenuFragments.swift` / `PanelVisibilityState.swift` / `UpdateChecker.swift` / `Info.plist` / `project.pbxproj` / `AppSettingsStoreTests.swift` + `AGENTS.md` + `Docs/27` + `Docs/28`——**下一个改其中任意一个的提交都会让它衰减**，要撤请趁早。**验证状态：656/656 单测全过（净 +26：新增 `StripContextMenuZoneTests` 9、`AccessoryWindowPresentationTests` 6、`SettingsCoordinatorTests` 10、`PanelVisibilityState` 抑制器 2，删除失去调用方的 `keyEquivalentPresentation` 与 `edgeTitle` 用例各 1）；Release 固定证书装机后用合成事件实测：右键→菜单 68–73ms（改前 158–176ms，对照组 chip 菜单 60–78ms）、菜单底边与任务条可视上沿差 0pt 且零重叠、右键两端空白弹钨极菜单而右键 chip 仍是它自己的菜单、`screencapture` 逐像素确认端点强调色生效；owner 实机验收通过（2026-08-04）** |
| `62e212e` 最大化避让做成菜单开关，默认关 | ✅ 单点成立（最新提交，按上面那段默认它随时会废；本次登记提交自身碰 `Docs/23` / `Docs/26` / `Docs/28`，本条都没带，暂时不自伤） | 2026-08-03 新增，登记当时 `git merge-tree` 退出码 0。**这是一条对用户可见的行为反转**：回退后避让重新变成恒开、不可关，菜单里「最大化窗口避开任务条」整项消失。用户已存的 `com.tungsten.edge.windowLiftEnabled` 键会变成孤儿键（不删，属于用户数据）——注意**回退后勾没勾过都一样恒开**，也就是主动关掉过的人会被重新打开，这是回退本条唯一会让用户"意外"的地方。碰了 `AppSettingsStore.swift` / `StatusMenuController.swift` / `AppDelegate.swift` / `WindowLiftAvoidanceController.swift` / `AppSettingsStoreTests.swift` + `AGENTS.md` + `Docs/27` 两个高频接触面——**下一个改它们中任意一个的提交就会让它衰减**（它自己正是这么打掉 `7b61767` 的）。回退不涉及避让本身的任何逻辑：`start()` 里只多一句 `guard isEnabledBySetting`，控制器其余部分一字未动。**验证状态：630/630 单测全过（新增 1 条）、Debug 构建 + AX 探针实测四条路径（默认关不抬 949pt / 勾上后 949→889 抬起 / 抬着的状态取消勾选 2 秒内 889→949 还原 / 再勾回来重新抬起）、跨重启保持、`DOCK_WINDOW_LIFT=0` 时勾着也不抬；owner 实机验收通过（2026-08-03，随 v0.7.4 发版前一并确认）** |
| `7b61767` 悬停设置改名为勾选项「鼠标悬停显示应用名」 | ⚠️ **已衰减**（真冲突只在 `App/Entry/StatusMenuController.swift` 一个文件；`Docs/27` 与 `AppSettingsStoreTests.swift` 能自动合）——2026-08-03 被 `62e212e` 压死，对照跑确认提交前还干净 | 2026-08-02 新增，登记当时 `git merge-tree` 退出码 0。**纯改名 + 菜单呈现层，行为与持久化格式零变化**：`HoverStyle` 枚举、raw value `standard`/`quiet`、键 `com.tungsten.edge.hoverStyle` 全部原样，回退不涉及任何数据迁移。回退后菜单从勾选项「鼠标悬停显示应用名」变回二级菜单「悬停效果 ▸ 标准 / 安静」（`HoverStyle.title` 随之复活），**用户存的档位原样保留、观感一致**——这是本表里少有的「回退只影响菜单长相、不影响任何行为」的条目。碰了 `AppSettingsStore.swift` / `StatusMenuController.swift` / `Docs/27` / `AppSettingsStoreTests.swift`，其中 `Docs/27` 是高频接触面。**注意它压在 `400bc9c` 之上**：要整体撤掉悬停设置这个功能，先 revert 本条再 revert `400bc9c`。**验证状态：629/629 单测全过（数量不增不减）、Release 构建 + 固定证书装机通过、真 HID 事件移指针的逐像素 A/B 复验（勾选态带标题卡 / 图标 / 文件夹格分别 21082 / 9582 / 10642 px，取消勾选后同坐标全 0 px，静息态两档 0/255）；owner 实机验收通过** |
| `400bc9c` 悬停效果两档设置（原「标准 / 安静」，现已改名） | ⚠️ **已衰减**（`AppSettingsStore.swift` + `StatusMenuController.swift` + `Docs/27-product-decisions.md` + `Tests/Unit/AppSettingsStoreTests.swift`）——2026-08-02 被 `7b61767` 压死，对照跑确认提交前还干净 | 2026-08-02 新增，登记当时 `git merge-tree` 退出码 0。**碰了 `AGENTS.md` 和 `Docs/27` 这两个高频接触面，外加 `DockStripView.swift` / `LauncherChip.swift` / `AppSettingsStore.swift` / `StatusMenuController.swift`——下一个改它们中任意一个的提交就会让它衰减**（它自己正是这么打掉 `0482a77` 的）。**现在要回退得先过 `7b61767`（改名那条），或手工解四处冲突**——改名动的正是本条建的那几处。回退后菜单里悬停这一项整项消失，全部 chip 回到只有一种悬停手感（图标 36→24 缩放 + 冒名字 + 文件夹格 1.12 放大 + 带标题胶囊底色提亮），带标题窗口卡悬停引起整行右移的老毛病也一并回来。用户已存的 `com.tungsten.edge.hoverStyle` 键会**变成无人读取的孤儿键**（不删，属于用户数据；重新引入本功能时会被原样读回）。**注意范围边界**：抽屉面板、抽屉入口胶囊、长标题全文浮层本来就不受本功能影响，回退不碰它们。**验证状态：629/629 单测全过、Release 构建 + 固定证书装机通过、真 HID 事件移指针的逐像素 A/B（安静档五类格子全 0 px vs 标准档 5911–22986 px；胶囊两档同为 3420 px；静息态两档 0/255；小档与特大档各复验一次）；owner 实机验收通过** |
| `0482a77` 拿掉图标的状态淡化 | ⚠️ **已衰减**（`App/Scenes/DockStripView.swift` + `App/Scenes/LauncherChip.swift` + `Docs/27-product-decisions.md`）——2026-08-02 被 `400bc9c` 压死，对照跑确认提交前还干净 | 2026-08-02 新增，登记当时 `git merge-tree` 退出码 0。**碰了 `AGENTS.md` 和 `Docs/27`（两个高频接触面），下一个改它们的提交就会让它衰减**。删掉整层：`DockIconDim` / `DockIconDimState` / `theme.iconDim(_:)` / `dockIconDim(_:)` / `LauncherChip.dimsWhenHidden` + 两套 token 的 `iconDimHidden` / `iconDimNotRunning`。回退后图标恢复按状态变淡（浅色降饱和、深色褪透明），也就是回到 owner 已经明确否掉的观感——**除非他改主意，否则不要单独回退它**。回退时注意它压在 `121aebf` / `385f228` 之上，那两条要恢复必须先过这一条。**现在要回退得手工解三处冲突**（`400bc9c` 在这两个视图里加了 `hoverStyle`/`showsHover`，在 `Docs/27` 的《界面外观》里加了新条目；两边改的是相邻但不同的东西，解冲突时保留 `hoverStyle` 那一路、只撤图标淡化即可）。**验证状态：单测全过、主 App Debug 构建通过、同一张 Finder 卡最小化前后逐像素 A/B（图标差 1/255、标题差 63/255）；owner 实机验收通过（2026-08-03，随 v0.7.4 发版前一并确认）** |
| `97dacd5` 交接目标不收无标题窗口 | ⚠️ **已衰减**（只冲突在 `Docs/28-process-pitfalls.md` 一个文件）——2026-08-02 被 **`adfb4a1`（上一轮的账本登记提交自己）** 压死，不是被后面的功能提交；对照跑坐实：对 `400bc9c` 退出码 0、对 `adfb4a1` 退出码 1 | 2026-08-02 新增，登记当时 `git merge-tree` 退出码 0。**衰减面只有文档**：源码那处 `guard title != nil` 与 `AGENTS.md` 都还干净，解冲突只需在 `Docs/28` 里挑出本条带的那段踩坑，运行行为的回退不受影响。**碰了 `AGENTS.md`（高频接触面），下一个改该文件的提交就会让它衰减**——`0482a77` 也改了 `AGENTS.md`，但改的是《Light / Dark Appearance》《Drag, Drawer》《Kept Apps》三节，与本条的《Focus And Action Planning》不重叠，三方合并过得去，**这次侥幸活下来，不代表下次**。源码只有 `AccessibilitySource.swift` 一处 `guard title != nil`，回退后飞书的隐形 layer-0 窗口会重新可能被选为最小化交接目标 → 飞书主窗口偶发被顶到最前。同提交带的 `Docs/27` 决策（最小化只影响被最小化的那个窗口；「兄弟窗口跳前」本轮不修）是文字，回退不影响运行行为。**验证状态：单测 630 全过；owner 实机验收通过（2026-08-03，随 v0.7.4 发版前一并确认）** |
| `385f228` 退出档不再全灰 | ⚠️ **已衰减，且回退它已无意义**（`DockThemeTokens.swift` + `DockThemeTests.swift` + `AGENTS.md`） | 2026-07-30 新增，2026-08-02 被 `0482a77` 压死——**对照跑确认过：`0482a77` 提交前它还干净，这是本表第四个「刚登记就被下一个提交打掉」的实例**。它调的那两格 token（`light.iconDimNotRunning` 从 `1.00/0.60` 改成 `0.58/0.72`）现在整个不存在了，所以就算解掉冲突也没有东西可调。要回到「退出档全灰」得先 revert `0482a77` 再 revert 本条 |
| `121aebf` 图标淡化转正 + 构建号 12 | ⚠️ **登记后隔一个提交就衰减**（`AGENTS.md` + `DockThemeTokens.swift` + `DockThemeTests.swift`）；2026-08-02 起**回退它也已无意义**——`0482a77` 把整层删了 | 上一行的 `385f228` 改了同样这几个文件——**登记时那句「随时会废」当天就应验了，这是本表第三个同模式实例**。要单独回退它，先 revert `385f228`。2026-07-30 新增。浅色图标淡化从纯 `.opacity()` 改为三通道（去饱和 + 压暗），owner 实机验收后转正为默认、`DOCK_ICON_DIM` 开关随之删除。回退后浅色图标退回「灰蒙蒙」（深底图标褪成灰方块），深色不受影响（它的两格本来就是冻结的历史字面值）；构建号退回 11，与当前已装包撞号。**它同时碰了 `AGENTS.md` 和 `project.pbxproj` 两个高频文件**，是下面 `c6dfdb7` / `b7abf7e` 两条当场衰减的原因 |
| `c6dfdb7` 构建号 10 → 11 | ⚠️ **下一个提交就衰减**（仅 `project.pbxproj`） | 上次复检写的「✅ 单点成立」**已不成立**：`121aebf` 又 bump 了一次构建号，改的是同样那四行。**源码零冲突**，手工把版本号解一下即可 |
| `b7abf7e` 效果改 opt-in | ⚠️ **下一个提交就衰减**（仅 `AGENTS.md`） | 上次复检写的「✅ 单点成立」**已不成立**：`121aebf` 往《Light / Dark Appearance》加了图标淡化那条护栏，压在同一节。**源码零冲突**。内容上仍然：回退后提饱和与厚度层恢复默认生效——也就是恢复到 owner 未认可的那个观感，**不要单独回退它** |
| `175cfa4` 浅色质感第二轮 | ⚠️ 不再是单点（8 个文件） | 被 `b7abf7e` 全面重写了同一批文件。要撤销提饱和/厚度层的能力本身，先 `git revert b7abf7e` 再 revert 本条 |
| `ed31ff2` 原生玻璃探路装置 | ⚠️ 不再是单点（`AGENTS.md` + `DockStripView.swift`） | 登记时是单点，被 `175cfa4`/`b7abf7e` 压中（都改了底板那几行与 AGENTS 同一节）。默认关，回退它不改变任何既有观感 |
| `f1efa7c` 浅色模式视觉适配 | ⚠️ 不再是单点（5 个文件 + `project.pbxproj`） | 登记时是 ✅ 单点，两个提交后就被 `ed31ff2` 压出冲突，此后又被第二轮层层覆盖。**整轮浅色要撤销就倒序连 revert**：`121aebf` → `c6dfdb7` → `b7abf7e` → `175cfa4` → `ed31ff2` → `f1efa7c` |
| `ec930ae` 辅助权限引导重建 | ⚠️ **一个提交之内就衰减**（仅 `AGENTS.md`） | 上一次复检写的「✅ 单点成立」**已经不成立**：紧接着的 `f923324`（纯文档，把考据搬去 `Docs/Archive`）只动了 AGENTS.md 10 行，就把它压出冲突。**源码零冲突**，手工解 AGENTS.md 即可 |
| `cfd6a71` 主线实例可信 | ⚠️ 不再是单点（`AppDelegate.swift` + `project.pbxproj`） | 2026-07-29 还是单点，`ec930ae` 重排了 `applicationDidFinishLaunching` 的启动顺序（位置分类要排在接管实例和注册热键之前），正好压在它改的那一段上。要回退得连 `ec930ae` 一起处理，或手工解这两个文件 |
| `d08e8d6` 彻底隐藏 / `15f8fd6` 文档追平 | ⚠️ 不再是单点（仅 `AGENTS.md`） | 同 `ec930ae`，被 `f923324` 的 AGENTS.md 搬迁压出冲突；源码零冲突 |
| `203b69c` 二维码 | ⚠️ **已衰减**（`README.zh-CN.md` + `assets/wechat-group.png`） | 2026-08-02 抽检发现：被 `edf9b64`（更新二维码 + 英文 README 补群入口）压中，改的是同样这两个文件。**不是本轮造成的——是上一次「48 条零新增衰减」的全表复检漏判了它**，`edf9b64` 在那次复检之前就已经存在。二进制图片在三方合并下必然冲突，回退时手工选一张即可，**源码零影响** |
| `42f70eb` 扫描准入 | ⚠️ 不再是单点（仅 `project.pbxproj` 一处） | **当天就衰减了**：早上复检还干净，`cfd6a71` 把 `BuildProvenance.swift` 加进工程文件、又 bump 了构建号，与 `42f70eb` 加 `ScanAdmissionDecision.swift` 的位置相邻。冲突只在 `macos-dock-cc-v2.xcodeproj/project.pbxproj`，**源码零冲突**，手工解一下工程文件即可，代价远小于下面那三条。（`f1efa7c` 又新增 3 个源文件，冲突范围不变，仍只此一个文件） |
| `8af961c` 启停首帧位置锚定 | ✅ 单点成立 | 跨多版本仍干净 |
| `bcef0ed` 菜单与唤醒快捷键调整 | ⛔ **退役，不要再执行** | 已被 `d08e8d6` 取代：它要撤销的「⌥⌘D 作为该命令快捷键展示」在当前代码里已不存在。冲突 6 个文件（`AGENTS.md`、`NativeDockPreferencesService.swift`、`Docs/05-known-platform-quirks.md`、两份 README、`AppSettingsStoreTests.swift`）。要回到那个行为，改为回退 `d08e8d6` |
| `b503fd6` POSIX 判活 + 释放日志 | ⚠️ 不再是单点 | 冲突 `AGENTS.md` + `ProcessLiveness.swift` + `project.pbxproj`。起因是 `42f70eb` 为做进程世代校验往 `ProcessLiveness.swift` 加了 `startTime`。真要回退需连 `42f70eb` 一起处理 |
| `a25add5` 消息纳入统一保留勾选 | ⚠️ 不再是单点 | 冲突 9 个文件（`AGENTS.md`、`AppMembershipController.swift`、`MessagingAppStore.swift`、`AppDelegate.swift`、`DockStripView.swift`、`LauncherChip.swift`、`project.pbxproj`、两个测试），跨版本自然衰减。数据边界（V3 键矩阵，见上方指针）仍然有效 |

**每次发版前跑一遍这个复检**，把结果日期更新到这里；发现某条退役就明确标掉，别留着让人以为还能用。

衰减比想象的快，而且有**两个**几乎必然踩到的公共文件：

- `project.pbxproj` —— `42f70eb` 在**同一天内**就从「单点」变成「要手工解工程文件」，起因只是又新增了一个源文件。几乎任何新增文件都会碰它。
- `AGENTS.md` —— 2026-07-30 这次复检发现的：`ec930ae` / `d08e8d6` / `15f8fd6` 三条在**上一次复检写下「✅ 单点成立」之后的第一个提交**里就全废了，而那个提交（`f923324`）是**纯文档**、只把考据搬去 `Docs/Archive`、AGENTS.md 净变化 10 行。凡是「功能 + 护栏同一提交」的条目都写过 AGENTS.md，所以任何一次 AGENTS.md 重排都会成片压中它们。

所以复检的正确频率不是「想起来时」，而是**每次发版前**，外加**任何一次新增源文件之后**，再加**任何一次改动 AGENTS.md 之后**——包括纯文档提交。

另外记一条方法上的教训：判断「是不是我这次改动造成的衰减」，不要只跑 `HEAD`，要拿**自己提交前的那个 tree** 跑同一遍对照（`git merge-tree --write-tree --merge-base=<提交> <我的提交>^ <提交>^`）。2026-07-30 这次一开始就误判成「浅色适配把三条压坏了」，对照之后才看清真凶是前一个纯文档提交，浅色适配零新增衰减。

**2026-07-31 机械复检**（触发条件：`683f1fc` 同时新增了源文件并改了 `AGENTS.md`，按上一段的规矩两条都得跑）。方法用的就是上一段那条对照命令，`HEAD~1` 与 `HEAD` 各跑一遍。结果：**`683f1fc` 零新增衰减**——复检前干净的 5 条（`385f228` / `698a266` / `7d1c8d0` / `8af961c` / `c9cb5a4`）复检后仍然干净，其余 34 条在 `683f1fc` 之前就已冲突。**但表里若干「↩ 单点」的实证已经过期**，例如 `ed31ff2` 标着「07-30 merge-tree 实测干净」而现在是冲突（衰减发生在 07-30 之后、`683f1fc` 之前）。本次只登记新行、不逐条重写旧判定——**逐条改写留给发版前的全表复检**。

**2026-07-31 启动崩溃修复后机械复检**（触发条件：`328bc59` 新增 `ManualPanelHostTests.swift` 并改了 `AGENTS.md`）。以功能提交本身为界，对既有 40 条逐条比较 `328bc59^` 与 `328bc59`：改前 6 条 clean / 34 条 conflict，改后 5 条 clean / 35 条 conflict。**唯一新增衰减是 `698a266`**，唯一冲突文件为 `App/Entry/PanelCoordinator.swift`；`AGENTS.md` 自动合并成功，其余 39 条状态不变。`328bc59` 自身模拟回退 exit 0，产出 tree `9519206ca713e90d8b8b2202da72257493389dbb`，可干净单点撤销。

**2026-07-31 承载器收尾后机械复检**（触发条件：`b2b6a38` 改了 `AGENTS.md`）。对照 `790e494` 与 `b2b6a38`：**唯一新增衰减是 `328bc59` 本身**——两者都改过 `App/Entry/PanelCoordinator.swift` 与 `AGENTS.md`，所以启动崩溃修复**不再是单点**，要退必须倒序 `git revert b2b6a38 328bc59`（已同步改写下方该条）。其余条目状态不变；`b2b6a38` 自身此刻是 `HEAD`，可干净回退。

**2026-07-31 文档索引后机械复检**（触发条件：`62136de` 改了 `AGENTS.md`）。对照 `ed85c3c` 与 `62136de`：**零新增衰减**——复检前干净的 6 条（`385f228` / `683f1fc` / `7d1c8d0` / `8af961c` / `b2b6a38` / `c9cb5a4`）复检后全部仍然干净。该提交只在 Source Of Truth 段追加一行索引，未触碰任何护栏正文。

**2026-08-01 启动弹跳修复后机械复检**（触发条件：`a645590` 同时新增源文件并改了 `AGENTS.md`，两条规矩都压中）。对照 `c45644a` 与 `a645590`：**零新增衰减**——复检前干净的 6 条（`385f228` / `683f1fc` / `7d1c8d0` / `8af961c` / `b2b6a38` / `c9cb5a4`）复检后全部仍然干净。它虽然重写了 `AppComposition.swift` 的启动会话段、`AGENTS.md` 追加了整节 *Launch Feedback*，但都落在既有条目改动区之外，`git merge-tree` 全部自动合并成功。`a645590` 自身模拟回退 exit 0，产出 tree `df7205bc1f204c8665502f6d4714070fe82ce5b6`，可干净单点撤销。

**2026-08-01 剪映 + 系统设置双修后机械复检 / 同时是 v0.7.3 发版前全表复检**（触发条件三条全中：`6414f9d` 新增源文件、改了 `AGENTS.md`、且是发版前）。对照 `3997247` 与 `6414f9d`，46 条逐条跑：改前 7 条 clean / 39 条 conflict，改后 6 条 clean / 40 条 conflict。**唯一新增衰减是 `a645590`**（`AGENTS.md` + `App/Composition/AppComposition.swift` + `project.pbxproj` 三处冲突，已同步改写下方该条的逆序命令）。其余 45 条状态不变；复检后仍干净的 6 条是 `385f228` / `683f1fc` / `7d1c8d0` / `8af961c` / `b2b6a38` / `c9cb5a4`。`6414f9d` 自身模拟回退 exit 0，产出 tree `5aa4a9da783514e5abdd4ea6592424d28dc8728f`，可干净单点撤销。表里 `f1efa7c` 在当前仓库已取不到（`git cat-file` 报 missing），本轮跳过，下次全表复检时确认它是不是该退役。

> 本轮方法上又踩一次坑，记下来：复检脚本第一版写成 `for c in $COMMITS`，**zsh 不对未加引号的变量做分词**，整张列表被当成一个字符串传进 `git cat-file`，于是一条都没真跑，脚本却"正常结束"并报出「零新增衰减」。差一点就把这个假结论登记进账本。→ 复检脚本必须**打印每条的判定行**并核对条目数，只看汇总数字发现不了空跑；循环用 `while read` 而不是 `for ... in $var`。

**2026-08-01 消息区图标尺寸修复后机械复检**（触发条件：`5f59c72` 改了 `AGENTS.md`；未新增源文件，也不是发版前）。对照 `5f59c72^` 与 `5f59c72`，42 条逐条跑并逐条打印判定：改前 6 条 clean / 36 条 conflict，改后**完全相同**——**零新增衰减**。复检后仍干净的是 `6414f9d` / `683f1fc` / `7d1c8d0` / `8af961c` / `b2b6a38` / `c9cb5a4`，另外单独补测了未被自动提取到的 `385f228`（改前改后均 clean，故实际干净的是 7 条）与 `a645590`（改前改后均 conflict，衰减早在 `6414f9d` 时发生，与本提交无关）。`AGENTS.md` 本次只在 *Taskbar Size Tiers* 段尾追加一条护栏，落在既有条目改动区之外，全部自动合并成功。`5f59c72` 自身模拟回退 exit 0，产出 tree `fa3f8c0af5e060e1a6d27cf458b8e591ddd1f614`，可干净单点撤销。

**2026-08-02 系统 Dock 菜单组重做后机械复检**（触发条件：`a6a31f7` 改了 `AGENTS.md`；未新增源文件，也不是发版前）。对照 `a6a31f7^` 与 `a6a31f7`，**48 条逐条跑并逐条打印判定**：改前 8 条 clean / 40 条 conflict，改后**完全相同**——**零新增衰减**。复检后仍干净的 8 条是 `385f228` / `5f59c72` / `6414f9d` / `683f1fc` / `7d1c8d0` / `8af961c` / `b2b6a38` / `c9cb5a4`。本次 `AGENTS.md` 改的是 *Settings And Compatibility* 段里系统 Dock 那两条的**正文改写**（不是段尾追加），但那几条的相关条目本就全是 conflict 状态，其余条目落在改动区之外、三方合并成功。另：上一轮记为「当前仓库已取不到」的 `f1efa7c` 本轮**可以取到并跑完**（结果 conflict），48 条无一跳过。`a6a31f7` 自身在临时 detached worktree 里 `git revert --no-commit` **无冲突**，且回退后工作树与 `a6a31f7^` **逐字节一致**（`git diff` 零行输出）。

| 目标 | 建议逆序 | 会保留什么 | 当前实证 |
| --- | --- | --- | --- |
| **撤销全屏 Space 互切 hold** | `git revert ef78c3e` | 保留标准绿灯 / `Control-Command-F` 输入前隐藏及其余全部功能；只删除已经确认全屏后切 Space 时的 generation hold 和 `120ms` 二次确认。相邻全屏应用互切会重新走 `.fullscreen -> CG false -> SHOW -> AX true -> HIDE`，已量到约 `2.2–11.3ms` 的任务条脉冲；普通 Space -> 全屏仍闪的既有边界不变 | **↩ 单点（2026-08-09 机械实测）**：临时 detached worktree 执行 `git revert --no-commit ef78c3e` 无冲突，`git diff --check` 通过。纯状态判定 + coordinator 接线 + 4 条单测，无 schema/UserDefaults 变化。正向全量 `707/707`，Release `arm64 + x86_64`；owner 在清理失败 prediction 后的 fresh fixed-certificate Release 验收三指与 Control-arrow 全屏互切均不闪、全屏返回普通 Space 正常。普通 -> 全屏仍 `3/3` 闪，未虚构为本提交已修 |
| **撤销原生全屏输入前隐藏** | 倒序两个：`git revert 93908d8 b4b07d0` | 保留 v0.7.6 及其余全部功能；删除 session event tap、AX 意图缓存、`.fullscreenTransitionPending`、设置项「进入全屏时避免闪烁」与中英文 README 的全局输入监听披露。标准绿灯 / `Control-Command-F` 进入原生全屏会恢复 v0.7.6 的一帧闪烁；Space 切换逻辑不变 | **↩ 两提交机械可退（2026-08-09 实测）**：rebased 后的 `93908d8`（收口）必须先退，再退引入完整 v1 的 `b4b07d0`；临时 detached worktree 执行 `git revert --no-commit 93908d8 b4b07d0` 无冲突，`git diff --check` 通过。无迁移；`com.tungsten.edge.fullscreenIntentEnabled` 回退后成为无害孤儿键，`DOCK_FULLSCREEN_INTENT` 无读取方。正向验证：全量 `703/703`，固定证书 universal Release；TextEdit 绿灯 / 快捷键 `0/3 + 0/3`，500 非目标按键开关 A/B 均 `500/500`、零 tap-disable。**注意**：本条不包含纯诊断归档提交 `0f9cab9` / `ff1192e`，回退功能后保留原始证据是有意行为 |
| **撤销窗口收编逐进程诊断** | `git revert 6bbe3fe` | 保留 WPS 重复兜底卡修复及此前全部行为；删除 `admissionProbe` JSONL 事件和 seed/scan 接线，`DOCK_SEED_AX_TIMEOUT_MS=0` 的 untimed 读重新只暴露空数组、不再保留 unread 结果供诊断 | **↩ 单点（2026-08-05 实测）**：`git merge-tree --write-tree --merge-base=6bbe3fe HEAD 6bbe3fe^` exit 0。纯观测提交，不改 100ms、补扫轮次或准入 verdict；日志仍默认关闭。全量 687/687；Release 固定证书装机。WPS 两臂实测：默认 100ms 与 untimed 都是 pid 40474 `success/raw=2/eligible=2/admit`、pid 47258 `success/raw=0/eligible=0/skipNoEligible`，当前现场未复现「整组不上条」，也不支持 timeout 根因 |
| **撤销多进程应用重复兜底卡修复** | `git revert 84157c9` | 保留逐进程诊断和此前全部功能；恢复按 PID 投影 `app-*` 的旧行为。WPS 一个有窗进程 + 一个零窗进程会在两张真实窗口卡旁重新多出一张「WPS Office」；多个同 bundle 零窗进程会重新把同一个 fallback id 追加多次 | **↩ 单点（2026-08-05 实测）**：`git merge-tree --write-tree --merge-base=84157c9 HEAD 84157c9^` exit 0。无 schema/UserDefaults 边界；7 条纯判定测试覆盖 bundle 抑制、全零去重、代表 PID、座位消失恢复和无 bundleID 同名隔离。装机实测最新投影只含 `tabgrp-40474-s9/s10`，无 `app-com.kingsoft.wpsoffice.mac` |
| **撤销跨重启 kept 排名 + 运行态 POSIX 对账批次** | 整批倒序：`git revert 6bbe3fe 84157c9 7bd9910`；若要保留后两项则需手工回退 `7bd9910` | 回到 v0.7.5 发布线行为：跨机器重启后的普通 kept 排名重新丢失，运行点重新只依赖 workspace 通知；同时删除该批次的顺序/座位释放诊断扩展。`stripKeptAppOrderV1` 留作无害孤儿键，数据边界见上方专节 | **⚠️ 已被后两提交压成非单点**：对当前 HEAD 单独模拟 revert，冲突在 `Docs/27-product-decisions.md`、`WindowInventoryAnomalyLog.swift`、其测试和 `project.pbxproj`；按左列整批倒序才是机械路径。功能提交时 678/678，owner 已实测跨重启顺序和退出运行点；当前整批 687/687 + Release 固定证书装机通过 |
| **撤销系统 Dock 菜单组重做（删显隐命令 + 滑杆确认后生效 + 自绘确认按钮）** | `git revert a6a31f7` | 保留其余全部功能。回退后**三件事一起回来**：① 菜单重新出现「隐藏 / 显示系统 Dock」命令与 `setAutohideEnabled(_:)`（只切 `autohide`、不碰 `autohide-delay`），分组标题行消失，`GlobalHotKeyShortcut.nativeDockAutoHide` 与 `AutoHideToggleMenuModel.shouldSkipNativeDockMenuAction` 一并复活——**owner 报的原始症状随之回来**：点了「隐藏系统 Dock」以为不会再被唤醒，鼠标一贴底边（任务条就在那儿）Dock 又弹出来。② 滑杆退回**松手即写**，那一下没有预告的 `killall Dock` 闪回来；键盘 / VoiceOver 路径退回 0.2s debounce 自动提交。③ 删除自绘的 `MenuActionButton` 与 `NativeDockApplyRowView`（连同 `acceptsFirstMouse` / `hitTest` / `trackEvents` 三处菜单内交互护栏）、`shouldShowNativeApply` / `nativeApplyTitle` / `delayDisplayName` 三个纯函数及其单测。`AGENTS.md` 对应的三条护栏与 `Docs/27` 的决策记录一并消失 | **↩ 单点（登记当天）**：本提交即 `HEAD`。**实证不止 merge-tree**——临时 detached worktree 里 `git revert --no-commit a6a31f7` 无冲突，回退后与 `a6a31f7^` 逐字节一致。**它改了 `AGENTS.md`**（改写 *Settings And Compatibility* 里系统 Dock 那两条正文并新增一条），下一个碰该文件的提交就可能压中；未新增源文件，`project.pbxproj` 未动。**无 schema 迁移，但有两处数据边界**：(a) `com.tungsten.edge.autoHide.nativeDock.lastEnabledDelay` 在主线上**已无读取方**（显隐命令是它唯一的消费者），字段与持久化都保留、不做有损清理，回退后立刻重新被读，值仍然有效；(b) 三个冻结的 `com.tungsten.edge.nativeDock.restoreDelay.*` 键继续不读不写不删。回退**不会**把 `com.apple.dock` 改回去，用户停在哪一档就还在哪一档。**验证状态**：全量单测 630/630；Release 固定证书装 `/Applications`，`lsappinfo` 确认 `type="UIElement"`；AX 枚举实证菜单结构（标题行 `enabled=false`、旧命令消失、无草稿时确认行不出现）、拖动后确认行浮出且系统 `autohide=1/delay=999` 纹丝未动、重开菜单草稿已弃；owner 于 2026-08-02 实机验收全流程并逐轮拍板按钮观感。**未覆盖**：确认按钮的**真实点击链路**（`mouseDown` 里那轮 `trackEvents`）没有自动化覆盖——合成点击拨不动菜单里的滑块（`Docs/28` 既有条目），单测只到 `onClick` 接线为止，按钮能不能真的按下去完全靠 owner 实机；键盘 / VoiceOver 调滑块后用回车确认这条路径也只有代码保证 |
| **撤销消息区图标尺寸修复（补上漏传的档位系数 + scale 改必填）** | `git revert 5f59c72` | 保留其余全部功能。回退后**两件事一起回来**：① 消息区「应用有主窗」那条渲染路径重新漏传档位系数 → 小 / 大 / 特大三档下，消息应用开着窗或最小化时图标小一号（恒按中档 44×52 渲染），关掉窗口反而正常；小档下 52pt 高的格子塞进 44pt 面板会被裁切。**中档不受影响**（系数 1.0 = 旧默认值，逐像素相同）。② `ChipView` / `LauncherChip` / `PinnedFolderChip` / `ShelfChip` / `DrawerDragIconView` 的 `scale` 重新带上默认值（1.0 / 0.7），漏传恢复为静默按默认值渲染而不是编译失败——**这正是本 bug 当初能溜过四档缩放那次改动的原因**；抽屉与 `drawerIcon` 载体那两处显式 `0.7` 退回隐式默认。`AGENTS.md` 的对应护栏一并消失 | **↩ 单点（登记当天）**：本提交即 `HEAD`，`git merge-tree` 模拟回退 exit 0，产出 tree `fa3f8c0af5e060e1a6d27cf458b8e591ddd1f614`。**它改了 `AGENTS.md`**（*Taskbar Size Tiers* 段尾追加一条），下一个重排该文件的提交就可能压中它；未新增源文件，`project.pbxproj` 未动。无 schema 或 UserDefaults 数据边界。**验证状态**：编译通过、全量单测通过；owner 于 2026-08-01 实机验收。**尺寸是量出来的不是看出来的**：特大档下同一帧里三种状态（飞书零窗走 `LauncherChip` / QQ 有窗走 `ChipView` / 微信最小化走 `ChipView`）相邻格子中心距均为 68.0pt，与 live 区 68.0pt 一致；修复前同样测法是 61.2pt（57.5 宽挨 44 宽）与 54.5pt（44 挨 44）。**未覆盖**：`scale` 是否真的传下去没有、也很难有单测——现有测试全是纯几何计算，**防回归完全靠「漏传即编译失败」这一个机制**，所以给这几个视图的 `scale` 加回默认值等于拆掉唯一的护栏；另外小档的裁切修复只有推导，没有单独实测（特大档已实测） |
| **撤销剪映 + 系统设置双修（启动目标包解析 + 空标题标准窗口准入）** | `git revert 6414f9d` | 保留其余全部功能。回退后**两件事一起回来**：① 启动一律用默认 `OpenConfiguration`，`allowsRunningApplicationSubstitution` 恢复默认开启 → 同 bundle id 存在冒名后台助手的应用（实证：剪映 `com.lemon.lvpro`，主体包内嵌两个 `LSUIElement` 助手，TrayHelper 以 `.accessory` 常驻）点图标打不开，因为 LaunchServices 拿助手顶替了主体；删除 `Core/Support/AppLaunchTargetDecision.swift` 及其单测，`--launch-trace` 的 `target=` / `returned=` / 两个 open 配置标志一并消失。② `.regular` 应用的窗口重新必须有标题才准入 → 系统设置 `com.apple.systempreferences` 退回应用级占位卡，**弹跳 20s、期间点击被吞、无法最小化三个症状同时回来**；`AppTrackerWindowEligibility` 一并消失，AX 清点路径退回给策略传 `appName=""` / `alpha=nil` / `executablePath=nil`（三条 deny 规则重新空转，飞书与无 bundleID 应用重新完全绕过策略），`AppEntry.executablePath` 与 `AppTrackerCGWindowSnapshot.alphaByWindowID` 变成无人读取 | **↩ 单点（登记当天）**：本提交即 `HEAD`，`git merge-tree` 模拟回退 exit 0，产出 tree `5aa4a9da783514e5abdd4ea6592424d28dc8728f`。**它改了 `AGENTS.md`、`project.pbxproj` 且新增了源文件**，下一个碰这三处的提交就会压中它（它自己已经把 `a645590` 压成非单点了）。无 schema 或 UserDefaults 数据边界。**验证状态**：全量单测 628/628；owner 于 2026-08-01 实机验收两个 bug 均已消失。**误收面是实测的**：本机 18 个前台应用全量扫描，「role=AXWindow 且标题为空」的只有系统设置一个。**未覆盖**：`AppLaunchTargetDecision` 的选包规则有 6 条单测，但「关掉 running substitution 后系统真的开出主体包」只有剪映一个实机样本，没有第二个同形态应用可测；alpha==0 过滤本轮首次在造座位路径上真正生效，无实测负样本，要观察的症状是「某应用的新窗口偶尔晚一轮才上条」 |
| **撤销启动弹跳修复（动画作用域 + 启动会话）** | 倒序两个：`git revert 6414f9d a645590` | 保留其余全部功能。回退后**三件事一起回来**：① 弹跳重新用 `repeatForever`，被 hover 事务（以及 `DockStripView` 的 `stripLayoutKeys`、`DrawerView` 的 `runningIDs`/`launchIDs`/`isPresented` 四处同类事务）顶掉后停死在 −6pt，即 owner 报的「鼠标移开就停在弹跳最高点」，图标尺寸同事务插值叠加成斜向乱跳；② 放行判定退回 `runningApplications.first(where: 同 bundleID)` + `activationPolicy != .regular`，启动早期 policy 未定型或同包名辅助进程命中即误放行（约 1 秒），兜底退回 8s 且两处计时器都不认会话，20 秒内二次启动会被旧计时器掐断；③ `RunningApplicationStore` 退回 `isTerminated` 判活、不再对未定型 policy 做有界重试，也不再处理「didTerminate 早于 POSIX 消失」——运行投影可能永久陈旧。删除 `Core/Support/LaunchGateDecision.swift` 及其单测、`--launch-trace` 模式与 `DOCK_LAUNCH_TRACE` 诊断 | **⚠ 已衰减为非单点（2026-08-01 当天，登记后一个功能提交）**：登记时是单点，紧接着的 `6414f9d`（剪映 + 系统设置双修）同时改了 `AGENTS.md`、`App/Composition/AppComposition.swift` 与 `project.pbxproj`——正是登记当时预判的那两个高频接触面，**一次全踩**。单独 revert 本条会在这三个文件上冲突，正确顺序见左列。无 schema 或 UserDefaults 数据边界。**验证状态**：全量单测 609/609；owner 于 2026-08-01 实机验收弹跳手感通过。**未覆盖**：`LauncherChip` 的计时器接线（创建 / 失效 / onAppear 重建）没有单测，纯判定 `LaunchGateDecision` 有 7 条；`RunningApplicationStore` 新增的两套重试全无单测，只有代码保证。**登记时那两个未解问题已于 2026-08-01 由 `6414f9d` 修复，且当时写下的两条猜测【都被实测推翻】**——留着这段是为了记住「猜测写进账本，下一轮会被当成事实用」：① 剪映当时怀疑「被误判为已在运行而走唤出分支，与本提交的 `RunningApplicationStore` policy 重试有嫌疑关联」，**不成立**，该 store 只收 `.regular` 而冒名助手是 `.accessory`，点击本来就走启动分支，真凶是 LaunchServices 按 bundle id 让已运行实例顶替了目标包；② 系统设置当时推测与 Apple「信息」同族（Catalyst + `AXDialog`），**也不成立**，它在 macOS 26 上链接 AppKit + Settings.framework，subrole 是正常的 `AXStandardWindow`，唯一异常是空标题 |
| **撤销面板承载器收尾** | `git revert b2b6a38` | 保留启动崩溃修复与全部产品功能。**纯行为中性**：拖动载体那段「已评估例外」的注释消失（下一个人会重新怀疑它是漏网之鱼）、胶囊的 `ManualPanelHost` 退回 `_ =` 丢弃（仅靠视图层级间接持有，给该类加 `deinit` 时会静默失效）、`AGENTS` 拆卸清单退回只列两个 host | **↩ 单点（登记当天）**：本提交即 `HEAD`。**它改了 `AGENTS.md`**，下一个碰该文件的提交就会压中它；它自己也已经把 `328bc59` 压成非单点。无 schema 或 UserDefaults 数据边界。验证状态：全量 592/592、Release 构建通过；**未装机验证**——三处改动均为注释 / 引用计数 / 文档，运行期行为不变，且当天反复重装曾导致辅助功能授权失配，故有意不再重装 |
| **撤销启动期面板布局循环修复** | 倒序两个：`git revert b2b6a38 328bc59` | 保留反馈计时器按需运行与 v0.7.1 全部产品功能；删除 `ManualPanelHost` 及其 7 项测试，主任务条、胶囊和 tooltip 恢复把 `NSHostingView` 直接装成 panel `contentView`，启动恢复「先显示、后异步收敛」，`$dockSize` 初值再次触发一次换档事务。**会重新暴露已复现的 AppKit Update Constraints 递归 / `NSGenericException` 启动崩溃** | **⚠ 已衰减为非单点（2026-07-31 当天）**：登记时是单点（merge-tree exit 0），随后 `b2b6a38`（承载器收尾）改了同一批文件（`PanelCoordinator.swift` / `AGENTS.md`），**单独 revert 本条会冲突**，正确顺序见左列。无 schema 或 UserDefaults 数据边界。验证状态：全量 592/592、Debug 构建、Release 固定证书安装与签名校验；当前双屏四档各 25 次、总计 100 次冷启动全部保持 `type="UIElement"`，每次 4 秒 CG 采样均看到两个合法面板，未出现 `53×100` 窄窗，崩溃报告 6→6。抽屉开合和底边跨屏往返实测通过；tooltip 用真实 `NSHostingView` 可见 display-cycle 单测覆盖。未单独实操自动隐藏、全屏和最大化避让，相关代码未改且全量回归测试通过 |
| **撤销反馈计时器按需运行** | `git revert 683f1fc` | 保留 v0.7.1 全部功能。反馈投影回到无条件 2Hz 发布——空闲期整条任务条 + 抽屉每秒重算两次，空闲 CPU 中位数从 1.40% 涨回 2.60%；删除 `FeedbackTickPolicy` 与 `Tests/Unit/FeedbackTickPolicyTests.swift`。**会一并丢掉一个顺手修的既有 bug**：`IntentFeedbackState.update` 退回无条件写，已兑现条目的 `updatedAt` 重新被每轮盖成 now、永远走不到 1.5s retention——在常驻计时器下**看不出来**（反正它一直转），所以回退后没有可观察症状，但那处 bug 又埋回去了，下次谁再做「计时器按需」会重新踩一遍 | **↩ 单点（登记当天）**：本提交即 `HEAD`，此刻 revert 必然干净。**它改了 `AGENTS.md` 且新增了源文件**，下一个碰这两处的提交就会压中它。无 schema 或 UserDefaults 数据边界。**验证状态**：全量 585/585、Debug + Release 双构建、固定证书装 `/Applications`、owner 于 2026-07-31 实机验收（重新授权后 30s 粗测中位 1.40%，与受控值吻合）。**−46% 的测法**：同日配对，两组各 90s 预热 + 155s 采样，单实例、鼠标离开任务条。**未覆盖**：Timer 的创建 / 失效 / stop 后异步回调没有直接的接线单测——纯判定（`FeedbackTickPolicy`）有单测，接线正确性目前靠端到端 CPU 实测反证（接错就不会掉 46%）；要真正锁住需给 `AppRuntime` 开注入缝 |
| **撤销任务条四档缩放** | `git revert 698a266` | 保留其余全部功能。菜单去掉「任务条大小 ▸」，面板几何回到写死的 52/92，`ShelfChip` / `PinnedFolderChip` 重新没有 `scale` 参数，胶囊和拖动载体不再观察 `AppSettingsStore`。**会一并丢掉一个顺手修的既有 bug**：条内标题宽度与 tooltip 截断阈值退回两处各写死 140pt（中档下行为相同，所以回退后看不出来，但那处耦合又埋回去了） | **⚠️ 2026-07-31 起非单点**：`328bc59` 在 `PanelCoordinator.swift` 上增加 HostingView 隔离和隐藏首帧事务，增量 merge-tree 实证本条从 clean 变为只冲突该文件（`AGENTS.md` 自动合并）。若要移除四档但保留启动崩溃修复，需手工解冲突并保留 `ManualPanelHost`、隐藏创建与同步首帧布局，几何固定回中档；不要靠先回退 `328bc59` 绕过，因为那会重新暴露已复现的启动崩溃。**UserDefaults 数据边界**：`com.tungsten.edge.dockSize` 会成为孤儿键（无害，不再被读）；回退时若用户停在非中档，任务条会**直接跳回中档**——这不是 bug，是该功能整体消失的必然结果，但要提前告诉用户。原验证状态：全量 575/575；四档逐档装机实测面板高 84/92/100/108、胶囊同宽同高，小档与特大档截图确认图标/运行点/分隔线/胶囊九宫格无裁切；owner 于 2026-07-30 实机验收通过。**未覆盖**：从菜单**现场换档**那条事务路径（代次门控 / 关抽屉重测 / 无动画原子提交）只有单测与代码保证，实测走的是「改配置 + 重启」，没有真机点过菜单 |
| **撤销「显示中转站」开关** | `git revert 2c0042f` | 保留其余全部功能。中转格恢复为恒显示的文件夹区固定头位，菜单去掉勾选项；`StripDropRouting` 的 `shelfFrame` 退回非可选、`headSlack` 消失（「插到第 0 位」重新只能靠中转格右侧那段 tailSlack）；`folderZoneMaxX` 退回非可选、nil 不再被当成「没量到」 | **↩ 需先退 C**：`698a266` 之后改过同一批文件（`DockStripView` / `StripDropRouting` / `AppSettingsStore`），**单独 revert 本条大概率冲突**，正确顺序是倒序 `git revert 698a266 2c0042f`。**UserDefaults 数据边界**：`com.tungsten.edge.showShelf` 成为孤儿键；若用户当时关着中转格，回退后它会重新出现（暂存的文件引用一直都在，不受影响）。全量 566/566；实测面板 722→657pt、胶囊同步左移，截图确认中转格连同多余分隔线一起消失；owner 于 2026-07-30 实机验收通过 |
| **撤销系统 Dock 滑块回归（回到彻底隐藏语义）** | 倒序两个：`git revert a6a31f7 8e6453e` | 保留其余全部功能。回退后：系统 Dock 滑块消失，菜单命令回到 `d08e8d6` 的「彻底隐藏」（隐藏时连 `autohide-delay=999` 一起写、显示时恢复隐藏前精确 delay），`⌥⌘D` 快捷键提示与菜单追踪期跳过逻辑再次移除，三个 `restoreDelay.*` 键重新被读写。**⚠️ 2026-08-02 起本条描述的「回退终点」已经变了**：`a6a31f7` 把这一组重做成「无命令 + 滑杆 + 确认按钮」，主线上**连显隐命令都没有了**，所以单独 revert 本条不再等于回到彻底隐藏语义，必须先退掉 `a6a31f7`（左列已改成倒序两个）。同理，本条原先那句「这条才是现在的活路径」现在指向 `a6a31f7` 那一行 | **⚠️ 已衰减为非单点（当天应验）**：登记时是 `HEAD`，随后批次 B（`2c0042f`）与 C（`698a266`）各改了一次 `AGENTS.md`，正如登记当时预判的那样压中了它——现在单独 revert 需手工解 `AGENTS.md`，源码部分（`StatusMenuController` / `AppSettingsStore`）也被 B/C 改过。要整轮退回请倒序 `git revert 698a266 2c0042f 8e6453e`。**系统侧数据边界**：回退不会把 `com.apple.dock` 改回去，用户当时停在哪一档就还在哪一档；冻结的 `restoreDelay.*` 键回退后会重新被读——若期间用户拖过滑块，那份陈旧快照与实际延迟已经不一致，回退后第一次点「显示系统 Dock」会按旧快照覆盖（present=0 时甚至会删掉 `autohide-delay` 键）。**验证状态**：全量测试 559/559、Debug 构建启动并达到 `type="UIElement"`、无崩溃报告；**菜单交互尚未 owner 实机验收**（滑块拖动、⌥⌘D 去重、四象限失败分支都只有单测覆盖，没有真机点过） |
| **撤销原生玻璃探路装置** | `git revert ed31ff2` | 保留全部功能与观感——它默认关闭（`DOCK_LIQUID_GLASS=1` 才开），回退等于删掉一个没启用的实验开关。删除 `App/Scenes/DockGlassBackdrop.swift`，`DockStripView` 底板改回直接用 `DockVisualEffectView`，AGENTS.md 去掉玻璃实测结论那两条 | **↩ 单点**：2026-07-30 `git merge-tree` 实测干净。全量测试 SUCCEEDED。**注意结论本身别一起丢**——「玻璃能采样窗口背后内容、但没有可测量的几何折射」是花了一轮实测换来的，回退代码前先确认 AGENTS.md 那两条已经搬到别处，否则下一个人会把这轮探路重跑一遍 |
| **撤销图标三通道淡化（只回图标，不动整轮浅色）** | 倒序两个：`git revert 385f228 121aebf` | 保留整轮浅色的其余全部成果。失去的是：浅色下变灰的图标退回纯 `.opacity()`（隐藏 0.45 / 退出 0.35），也就是 owner 报的「灰蒙蒙」重现，深底图标重新褪成灰方块；两档重新只靠那点透明度差区分。**深色观感不受影响**——它的两格本来就是冻结的历史字面值。构建号退回 11，与当前已装包撞号 | **↩ 单点**：2026-07-30 `git merge-tree` 实测干净（登记当天即最新提交，随时会被下一个碰 `AGENTS.md` 或 `project.pbxproj` 的提交压废）。全量测试 SUCCEEDED（556 条）。**验收依据是实机**：装 `0.7.0 (12)` 后不带任何环境变量正常启动，owner 认可；代码侧另有证明——`legacyHidden`/`legacyNotRunning` 现在只被深色列引用，浅色没有任何路径能回落到旧值 |
| **撤销整轮浅色（推荐用这条，不要挑单个提交）** | 倒序连着 revert：`git revert 385f228 121aebf c6dfdb7 b7abf7e 175cfa4 ed31ff2 f1efa7c` | 回到 `f923324` 的浅色状态：白字白框黑阴影全部回来，深色不受影响。同时失去材质/提饱和/厚度层三个调参开关、玻璃探路装置，以及图标淡化的三通道处理（图标退回纯 `.opacity()`，浅色下重新「灰蒙蒙」） | **⇤ 整组**：这一轮 9 个提交层叠很厚（同一批视图文件被反复改），逐个单独 revert 基本都会冲突，2026-07-30 全表复检已逐条确认。整组倒序是唯一低摩擦路径 |
| **撤销浅色模式视觉适配（单条，仅供参考）** | `git revert ed31ff2` 之后再 `git revert f1efa7c`（**必须按这个顺序**；2026-07-30 复检后此路径已被上方整组取代——`175cfa4`/`b7abf7e` 又覆盖了同一批文件） | 保留 v0.7.0 全部功能；**深色观感不受影响**（它的数值本来就是这次冻结进 token 表的历史值）。失去的是浅色模式的单独适配：窗口标题与悬停应用名退回写死的白字、运行小圆点退回白点、区域分隔线退回白线——在浅色底板上全部重新变成看不清；面板描边退回均匀一圈白（浅色下呈现为用户报的那圈灰"方格"）；阴影退回黑 0.35/r15/y8（浅色底上的灰污渍，且会透过半透明底板压暗条的下缘）。`DockVisualEffectView.updateNSView` 退回空实现，外观切换时材质不再更新 | **↩ 单点，无 schema 或 UserDefaults 数据边界**：删除 `Core/Support/DockThemeTokens.swift`、`App/Scenes/DockTheme.swift`、`Tests/Unit/DockThemeTests.swift`，9 个视图文件恢复约 50 处写死字面值，AGENTS.md 去掉 *Light / Dark Appearance* 一节。2026-07-30 `git merge-tree` 实测干净单点。全量测试 SUCCEEDED（含新增 `DockThemeTests` 14 条冻结深色 38 个字段）；**深色未变是逐像素证明的**——已装旧 Release 版与新构建同姿势截图，最大单通道差 11/255、>8 的像素 12/299200（0.004%，残差是两次截图之间背后内容变化）；浅色量化：窗口标题最暗 10% 均值 148.7 → 34.2、运行圆点最亮 237.9 → 最暗 75.1、上沿描边 172 → 216、下沿描边 160 → 140；外观切换实时跟随无需重启。**未覆盖**：抽屉、两个弹窗、tooltip、拖动浮动卡片未逐一截图核对（用的是同一批已验证的 token）；浅色数值待 owner 逐项拍板，属半成品的第一步 |
| **撤销辅助权限引导重建** | `git revert ec930ae` | 保留 v0.7.0 全部功能；退回 61 行的基础版权限页。失去三件事：① 不再识别「从 DMG 直接运行 / App Translocation」——那种临时副本会重新去接管正在工作的 `/Applications` 实例并抢注全局热键；② 不再有 8 秒后的条件式排障说明（勾着却不生效时删旧条目重加）；③ **运行期权限失效重新变成静默空任务条**，且最大化避让的窗口快照会在失权瞬间被清掉、永远还不回去 | **⚠️ 已衰减为非单点（仅 `AGENTS.md`，源码零冲突）——登记当时是单点，`f923324` 的 AGENTS.md 搬迁压中了它**，无 schema 或 UserDefaults 数据边界：删除 `Platform/Permissions/` 下 4 个新文件与 `Tests/Unit/PermissionOnboardingTests.swift`，恢复 `AppDelegate` 启动分支、`WindowLiftAvoidanceController` 的冻结闸门与 `pendingRestorations`、`PanelCoordinator.suspendAndRelease()`。**注意 `cfd6a71` 因本提交衰减为非单点**（见上方复检表），两者的 `AppDelegate` 改动相邻。全量测试 524/524、Debug/Release/window-lab 三项构建通过；owner 于 2026-07-30 在**另一台装有已发布 0.7.0 的 Mac 上覆盖升级实测**走通完整引导（真实的「勾着却已失配」旧记录，`tccutil reset` 复现不出这个状态），并已用 `install_local_release.sh` 装到主力机、`lsappinfo` 确认 `type="UIElement"`（固定证书下 cdhash 变化但授权未丢）。**未覆盖**：可写外置卷放行、macOS 12 文案分支；运行期失效与重启失败重试两条为 owner 主动缩减范围，未实测 |
| **撤销主线实例可信改造** | `git revert cfd6a71` | 保留 v0.7.0 全部功能；失去三件事：菜单版本行不再标「· 开发版 / · 非安装位置」、启动时不再终止同 bundle id 的其他实例、`build_and_run.sh` 退回 `open -n`（强制多开）。开发线构建号退回 8，与已发布的 `0.7.0 (8)` 重新撞号 | **↩ 单点，无代码数据边界，但有机器状态边界**：回退**不影响**已经修好的系统侧状态——登录项仍指向 `/Applications/Tungsten Edge.app/`、`/Applications` 里仍是唯一一份 0.7.0，这些是本机操作的结果、不由代码控制。真正失去的是**防复发能力**：回退后若再在开发构建上勾选「登录时启动」，会重新掉进 2026-07-21 / 07-26 那两次误诊。删除 `Core/Support/BuildProvenance.swift` 及其 7 项单测。全量测试 493/493、单实例接管已实机验证（强开第二个实例后 4 秒内只剩新的）、owner 于 2026-07-29 验收 |
| ~~**撤销系统 Dock 彻底隐藏改造**~~ | ~~`git revert d08e8d6`~~ | **本条已作废（2026-07-30）**：owner 推翻了 2026-07-29 的彻底隐藏决定，主线「系统 Dock 滑块回归」批次已把命令改回只切 `autohide`（严格等价 ⌥⌘D）、恢复 `⌥⌘D` 快捷键展示与菜单追踪期跳过逻辑，并新增系统 Dock 唤醒延迟滑块——`d08e8d6` 引入的行为**已经不在主线上了**，再 revert 它只会把滑块路径一起打乱。要回到彻底隐藏语义，得 revert 那个新批次而不是这一条（新批次的回退命令待检查点登记） | **⛔ 失效**：原验证（全量测试 486/486、owner 2026-07-29 实机验收）针对的行为已被主线取代。原有的系统侧数据边界现在由滑块本身承担：升级后若 Dock 正处 `autohide-delay=999`，滑块会如实停在「不唤醒」，拖回来即可。三个恢复快照键 `com.tungsten.edge.nativeDock.restoreDelay.*` 按 owner 决定**冻结保留**（不读不写不删），不是孤儿清理对象 |
| **撤销扫描准入 ScanAdmissionDecision 修复** | `git revert 42f70eb` | 保留 v0.7.0 其余全部功能；补扫轮次恢复用原始 `AXWindows` 非空作为准入依据，Tailscale 这类应用会重新出现「永不消失的空图标」 | **↩ 单点，无 schema 或 UserDefaults 数据边界**：移除新增 `Platform/AppTracking/ScanAdmissionDecision.swift` 与其单测，恢复 `AppTracker` 补扫路径的旧准入与第二次无超时 AX 读取。注意误准入是**永久性**的——`reconcile()` 只清理已死进程，回退后已产生的空图标要重启钨极才会消失。全量测试 486/486、owner 于 2026-07-29 实机验收通过 |
| **撤销菜单与唤醒快捷键调整** | `git revert bcef0ed` | 保留正式版 `v0.6.6@dd85d86` 全部功能；移除「打开系统 Dock 设置…」，钨极快捷键恢复为 ⌥⌘E，消息成员菜单恢复「标记 / 取消标记」动态标题。没有新 UserDefaults key 或迁移；回退不会反转用户已经通过既有 kept / messaging 菜单做出的选择 | **↩ 单点，无 schema 数据边界，已验证 revert**：临时 detached worktree 执行 `git revert --no-commit bcef0ed` 无冲突，工作树与索引均和 `dd85d86` 完全一致；定向测试 84/84、Debug 全量测试 474/474、Debug 构建与唯一实例启动通过；owner 于 2026-07-25 实机验收通过；`git diff --check` 无输出 |
| **撤销 POSIX ProcessLiveness 修复与批量座位释放日志** | `git revert b503fd6` | 保留 4.4 及此前全部功能；`reconcile()` 恢复 `NSRunningApplication(processIdentifier:) == nil` 判死路径（LaunchServices 解析瞬时返回 nil 误杀活进程，连带移除整 `AppEntry` 与全部座位），同时失去 `.processGone` 批量释放日志 | **↩ 单点，无 schema 或 UserDefaults 数据边界，已验证 revert**：移除新增 `Platform/AppTracking/ProcessLiveness.swift`，恢复 `AppTracker.reconcile()` 中 `NSRunningApplication` 判死与无日志批量释放路径；`/usr/bin/true + posix_spawn + waitpid` 实证 POSIX `kill(pid, 0)` 与 LaunchServices 行为差异；定向测试 26/26、Debug 全量测试 468/468、主 App Debug/Release + Window Lab Debug 三项构建通过；owner 于 2026-07-25 实机验收通过；临时 detached worktree `git revert --no-commit b503fd6` 无冲突。安装回退见上方当前本地主线节 |
| **撤销稳定重建的消息区主窗口能力准入门槛** | `git revert aad543d` | 保留「退出 App」菜单末项及此前全部功能；恢复仅凭白名单 / 社交类别自动注册消息应用、不检查主窗口标题的旧规则 | **↩ 单点，无 schema 迁移，已验证 revert**：从旧实现 `5e2e82e` 恢复纯 `MessagingZoneAdmission`、自动注册参数与 snapshot 标题输入，并同步新增两条能力护栏、删除一条 kept 冲突旧护栏；既有 persisted messaging / opt-out 数据不由回滚改写，回滚只影响后续自动准入；定向测试 41/41、Debug 全量测试 456/456、主 App Debug/Release + Window Lab Debug 三项构建通过；owner 于 2026-07-24 实机验收通过；临时 detached worktree `git revert --no-commit` 无冲突，回退后 9 个文件与父提交 `fc2eeee` 完全一致。安装回退见上方当前本地主线节 |
| **撤销稳定重建的「退出 App」菜单末项修复** | `git revert 3bd1fef` | 保留未启动应用「打开」、消息区退出态灰显及此前全部功能；恢复部分运行态 chip 菜单把成员项排在退出之后的旧顺序 | **↩ 单点，无数据边界，已验证 revert**：从旧实现 `5535657` 恢复纯菜单排序改动与 AGENTS 护栏；`LauncherMenuPlan`、app-level fallback 和具体窗口菜单统一为动作项 → 成员项 → 退出，分隔线由退出分支自补；定向测试 6/6、Debug 全量测试 442/442、主 App Debug/Release + Window Lab Debug 三项构建通过；owner 于 2026-07-24 实机验收通过；临时 detached worktree `git revert --no-commit` 无冲突，回退后 5 个文件与父提交 `aedfb37` 完全一致。安装回退见上方当前本地主线节 |
| **撤销稳定重建的未启动应用「打开」+ 消息区退出态灰显** | `git revert d226e80` | 保留启停首帧影子滑动修复及此前全部功能；恢复未启动图标右键无「打开」、消息区退出态常亮的旧观感 | **↩ 单点，无数据边界，已验证 revert**：从旧实现 `a18c8aa` 恢复纯 UI / 菜单层改动，新增纯 `LauncherMenuPlan.open` + `LauncherChipVisualPlan`（均有单测），并收窄 `dimsWhenHidden` 与启动动作分流；定向测试 9/9、Debug 全量测试 441/441、主 App Debug/Release + Window Lab Debug 三项构建通过；owner 于 2026-07-24 实机验收通过；临时 detached worktree `git revert --no-commit` 无冲突，回退后 7 个文件与父提交 `5be8013` 完全一致。安装回退见上方当前本地主线节 |
| **撤销稳定重建的启停/重开首帧影子滑动修复** | `git revert 8af961c` | 恢复到 v0.6.5 稳定基线的排序行为，其余功能不变 | **↩ 单点，已验证 revert**：从 `0b74fa6`（原 `0a70130`）恢复 absent rank-anchor；功能 + AGENTS 护栏 + 测试同一提交；定向测试 23/23、Debug 全量测试 437/437、主 App Debug/Release + Window Lab Debug 三项构建通过；owner 于 2026-07-23 实机验收通过；临时 detached worktree `git revert --no-commit` 无冲突，回退后四文件与 `v0.6.5@9b4b5d0` 完全一致。安装回退见上方稳定重建基线节 |
| **撤销消息应用纳入统一保留勾选** | `git revert a25add5` | 抽屉图标反馈、抽屉/保留拆分及此前全部功能；恢复消息应用永久身份、消息与 kept 互斥、消息只显示取消标记的旧语义 | **↩ 单点，带三键数据边界**：功能 + 纯投影 + 纯菜单投影 + 迁移 + 测试 + AGENTS 护栏同一提交；431 项单元测试、macOS 12 通用构建（arm64 + x86_64）、`git diff --check`、owner 真机验收（消息应用保留勾选、取消勾选后退出消失、迁移观感不变）通过；未单独验证 revert |
| **撤销抽屉图标悬停/点击反馈与九宫格缩小** | `git revert 8e2b851` | 抽屉位置与保留状态拆分及此前全部功能 | **↩ 单点**：独立视觉优化提交；仅 `DrawerCapsuleButton` 内层九宫格 scaleEffect 反馈（悬停 1.07 / 点击 0.93 回弹）+ 网格常量（icon 9 / 间距 4 / 圆角 iconSize÷4），macOS 12 Debug 通用构建与 owner 实机验收通过，未单独验证 revert |
| **撤销抽屉位置与程序坞保留状态拆分** | `git revert 5f5efa0` | 精简状态栏菜单与此前全部功能；恢复抽屉成员即保留成员、动态“在程序坞中保留 / 从程序坞中移除”菜单和拖拽时同步改 kept 的旧语义 | **↩ 单点，带上述数据边界**：功能、纯投影与排序决策、迁移、测试、工程护栏同一提交；413 项单元测试、macOS 12 Debug 通用构建（arm64 + x86_64）与 `git diff --check` 通过。新产物已启动，原生菜单与迁移行为仍待 owner 完整实测验收；未单独验证 revert |
| **撤销精简状态栏菜单** | `git revert 841fe45` | 自动隐藏快捷键与此前全部功能；状态菜单恢复系统 Dock 延迟滑杆、两组自动隐藏勾选和“添加固定文件夹…”入口 | **↩ 单点**：菜单结构、系统 Dock 显隐写入、测试、README 与工程护栏同一提交；406 项单元测试、macOS 12 Release 通用构建（arm64 + x86_64）、Debug 启动、`git diff --check` 与 owner 真机验收通过。系统 Dock 动态命令只写 `autohide`、保留原 `autohide-delay`。未单独验证 revert |
| **撤销菜单栏自动隐藏开关与钨极快捷键** | `git revert 36c23c0` | 贴底边热区外闪烁修复与此前全部功能 | **↩ 单点**：功能、可靠性修复、测试、README 与 shared scheme 同一提交；408 项单元测试、macOS 12 Release 通用构建（arm64 + x86_64）、Debug 启动验证通过。owner 实测菜单关闭时 ⌥⌘E / ⌥⌘D 均有效；菜单展开时 ⌥⌘E 无效为已接受边界、直接点击菜单项，系统 ⌥⌘D 仍有效。未单独验证 revert |
| **撤销贴底边热区外闪烁修复** | `git revert 3b3c9a4` | 最大化避让全功能与此前全部功能 | **↩ 单点**：纯规则 + 调用点 + 单元测试的自包含提交；372 项单元测试、Debug 构建、`git worktree` 建改前基线做实机对照复现（外接屏底边角落停留 23 秒：改前 69 行日志 64 次唤醒/隐藏交替，改后同操作零次）、999 不唤醒模式复测通过，未单独验证 revert |
| **撤销避让响应节奏调优**（回到 1.5s/6s 保守节奏） | `git revert c9cb5a4` | 最大化避让全功能与此前全部功能 | **↩ 单点**：纯常量/确认逻辑调优；371 项单元测试、Debug 构建、双屏实测（首抬 ~0.6s、放弃态自愈 ~1.4s）通过，未单独验证 revert |
| **撤销整个最大化窗口避让** | `git revert 8c60676 c9cb5a4`（连同节奏调优一起） | 回滚账本、文档入口与此前全部功能 | **↩ 单点**：功能自包含（3 新文件 + AXWindowReader 写机器 + PanelCoordinator accessor + AppDelegate 装配 + AGENTS 护栏段）；371 项单元测试、macOS 12 Debug/Release 构建、双屏（Retina 内建 + 1x 外接）连续缩放实测通过，未单独验证 revert。旧对照试验仍封存在 `experiment/avoid-scale-hover @ 4b58365` |
| **撤销文档入口与回滚账本** | `git revert d2247a1` | 全部产品功能 | **↩ 单点**：纯文档提交（Obsidian 首页指向 + 本账本文件），未单独验证 revert |
| **撤销窗口身份持久诊断日志** | `git revert 489f5d5` | 现有折叠四级判断、影子池、幽灵自愈五道门槛和此前全部功能 | **↩ 单点**：纯观测检查点，不改变窗口行为；333 项单元测试、Debug / Release 构建、LaunchServices `/dev/null` 启动和三实例并发写入验证通过，未单独验证 revert |
| **撤销窗口标题悬停提示** | `git revert 3d1c280` | 菜单栏手动检查更新和此前全部功能 | **↩ 单点**：独立功能提交；314 项单元测试、macOS 12 Debug 构建、Tooltip 单行/两行渲染和 owner 实机验收通过，未单独验证 revert |
| **撤销菜单栏手动检查更新** | `git revert 1aced10` | 影子标签池、幽灵座位自愈和此前全部功能 | **↩ 单点**：独立功能提交；305 项单元测试、Debug 构建与 owner 实机验收通过，未单独验证 revert |
| **撤销影子标签池 + 幽灵座位自愈** | `git revert 2a00e2d` | 权限引导窗口和全部功能 | **↩ 单点**：独立修复提交；含影子标签池和幽灵座位自愈单元测试，未单独验证 revert |
| **恢复避让试验到主线**（找回，不是撤销） | `git merge experiment/avoid-scale-hover` 或挑单个提交 `git cherry-pick 229a6a5 6ca3992 4b58365` | 主线当前全部功能 + 避让试验代码 | **⚠ 人工（已基本失去意义）**：`8c60676` 已把避让正式版带上主线（AX 写机器即取自 `229a6a5`），再合旧试验会在 `AXWindowReader`/`AppDelegate`/工程文件冲突；分支仅作历史参考保留 |
| **撤销权限引导窗 + 本地重签** | `git revert 9f10a14` | 全部主链功能（避让试验已经不在主线上，不受影响） | **↩ 单点**：工程支撑与发布提交；未单独验证 revert |
| **撤销固定文件夹 chip 悬停放大反馈** | `git revert 7d1c8d0` | 外部文件移入文件夹和全部功能 | **↩ 单点**：独立视觉优化提交；纯 scaleEffect 叠加，未单独验证 revert |
| **撤销外部文件移入文件夹** | `git revert 22d22a0` | 固定文件夹 chip 悬停放大反馈、消息身份常驻、移除门控、弹窗落头和全部可靠性优化 | **↩ 单点**：独立功能提交；283 项单元测试、Debug 构建通过 |
| **撤销低风险代码清理** | `git revert 871305d` | 两项性能优化和全部产品功能 | **↩ 单点**：独立清理提交；254 项测试、Debug 构建和 owner 实机验收通过 |
| **撤销删除 WorkspaceSource** | `git revert 1d4f4ca` | 低风险代码清理和全部产品功能 | **↩ 单点**：独立清理提交；254 项测试、三 target 构建通过 |
| **撤销消息身份常驻与移除门控** | `git revert 279935e` | 删除 WorkspaceSource、低风险代码清理和全部产品功能 | **↩ 单点**：独立提交；264 项测试、Debug 构建通过 |
| **撤销悬停事件驱动改造** | `git revert 6777b6a` | CG 查询优化和全部产品功能 | **↩ 单点**：独立性能提交；254 项测试、Debug 构建和双屏实机行为通过 |
| **撤销单轮 CG 快照复用** | `git revert 4ffd99f` | 全部产品功能和成员语义 | **↩ 单点**：独立性能提交；254 项测试、Debug 构建、WindowLab 与 Ghostty 实测通过 |
| **撤销最新成员语义与消息区双向拖拽** | `git revert e123856` | 固定应用最终行为、`2d97005` 多标签修复、固定区与内容预览 | **↩ 单点**：无冲突；撤销后 214 项测试与 Debug 构建通过 |
| **移除整套固定应用 / 程序坞成员演进（G2）** | `e123856 → da4a39d → 44856eb → f9a3476` | 固定文件夹、内容预览、任务条细节和 `2d97005` 多标签修复 | **⚠ 人工**：首个提交可撤销，继续到 `da4a39d` 出现冲突；不能把这串 hash 当成可直接执行的命令 |
| **移除多标签成员历史折叠** | `2d97005` | 固定应用与最新成员语义 | **⚠ 人工但功能独立**：直接撤销会在工程文件和护栏文档冲突，不并入 G2 |
| **移除 Finder / 固定文件夹内容预览** | `c9fad7d` | 固定文件夹区、中转格和后续程序坞成员能力 | **⚠ 人工**：直接撤销会在拖拽、任务条和工程文件冲突 |
| **移除抽屉 Card Nav 入场动画** | `1821274` | 抽屉功能及后续成员关系 | **⚠ 人工**：直接撤销会在现行抽屉视图冲突 |
| **移除整套固定文件夹能力（G1）** | `c9fad7d → 7c6acb1 → f2f2123 → bf5fb32 → 55894b4 → 48a2415 → b145b1c → 0ae5348 → 3fa4404 → d7ae76e` | 核心任务条、抽屉和程序坞成员能力 | **⚠ 人工**：这是目标顺序，不是已验证命令；后续任务条与拖拽重构会产生冲突 |
| **恢复垃圾桶旧方案** | 不直接 revert；只从 `d7ae76e` 查阅旧实现 | 当前固定文件夹能力不变 | **⛔ 已反转**：`3fa4404` 明确砍除，恢复需要重新评估常驻负担与权限 |

## 相关历史

- 主线撤销避让试验：`9ff1aec → 299748a → 63f7ee8`（07-13）。撤销 `229a6a5` 时与 `4b58365` 冲突：AppDelegate 里只调用两个避让控制器 `stop()` 的收尾方法一并删除，已人工解决。完整试验封存在 `experiment/avoid-scale-hover @ 4b58365`。
- 远端 `origin/master@121724d` 保留旧 v0.5.0 发布合并历史，相对当前本地稳定主线有 3 个远端独有提交；2026-07-24 提升本地 `master` 时未 fetch、未 push、未改 `origin/HEAD`。
