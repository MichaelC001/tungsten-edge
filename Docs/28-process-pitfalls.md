# 28 · 流程踩坑（工作方式）

> 只记**工作方式**上栽过的跟头——分支、打包、验收、版本。这类坑不读代码也会踩，而且重复代价高。
> 三条同时成立才补一条：不读代码的人也会踩、会重复发生、这次已经真的浪费过时间。默认不写。
>
> 某个应用有什么毛病 → 看 Obsidian `进度看板.html` 里状态为 🐛 的卡片；代码实现层的坑在本目录其他文档，不重复搬。
>
> 2026-08-01 从 owner 的 Obsidian 库 `04 流程踩坑.md` 迁入。原件存 Obsidian `Archive/2026-08-01 文档瘦身/`。

## 分支与发布

**发布分支手工挑提交，一定会漏。**
修好的功能像是"又坏了"，实际是那段代码从没进过发布分支——发布分支从更早的位置手工挑了几个提交拉出去，漏掉两个功能提交，日常用的包因此一直缺它们。
→ 开发布分支**从主线直接拉**；万一手工挑了，拉完对一次差异。（2026-07-20 吃过）

**发布之后主线的版本号不动，于是"用户装的包"和"我这台机器上跑的包"版本号一模一样。**
用户报「菜单里没有『打开系统 Dock 设置…』」，我这边打开菜单明明有，两边版本号都显示 `0.6.6 (7)`——光看版本号完全查不出差别。实际是那个功能的提交比发布晚了 14 小时，从没进过任何安装包。整轮排查都花在确认"到底是不是同一份代码"上。
→ 遇到「用户说没有、我这儿有」这类问题，**先比对提交，别信版本号**。要根治就在发版后顺手把内部构建号往前推一位（版本名不变），让开发构建显示 `0.7.0 (9)`、用户装的是 `0.7.0 (8)`，一眼可分。（2026-07-29 吃过）

**提交被"回摘"到新分支后，旧编号就失效了。**
回滚账本里如果记的还是旧编号，将来照着撤销会落空。
→ 回摘后在账本里记**新编号**，并注明原编号可追溯。

**回退重建时只顾着"功能加回来了没有"，会把和故障无关的基建一起丢掉。**
从 v0.6.5 一点一点重建时，注意力全在产品功能上，结果签名+公证打包链、配套的权限重授权引导、试用包脚本这些**跟当初那次故障毫无关系**的东西留在了废弃分支上没跟过来。等到发现，v0.6.6 已经以未签名未公证的形式发出去了，用户得右键才能打开——而这套东西当时明明已经做好了。
→ 回退之前先把要重建的清单列全，**分两类**：出问题的功能，和被顺带牵连的基建（打包、签名、权限、脚本、工程配置）。第二类最容易漏，因为它平时不出现在界面上。重建收尾时对着两条线各点一次名。（2026-07-26 吃过）

**笔记里写"不采用"，代码里它还在跑——两边会悄悄漂移。**
进度笔记把一个修复标成了「owner 不采用」，实际那个提交一直在主线里，连已发布的版本都含着它。这种矛盾平时看不出来，等到要基于笔记做决定时才会误导人。
→ 涉及"某个东西到底采没采用"的结论，写进笔记前先在代码里确认一次；发现两边打架时**先问清楚，不要自行判断哪边对**。（2026-07-26 吃过）

## 打包与安装

**修完不重新打包，等于没修。**
日常在用的包落后于代码时，报上来的 bug 可能早就修好了，白查一轮。
→ 修完一批就换包；报 bug 前先确认手上的包是不是最新的。（2026-07-20 吃过一次完整的）

**日用一定要用 Release 包，Debug 包只配用来验证功能对不对。**
开发用的 Debug 包没有编译器优化，体积是 Release 包的两倍多（实测 16.9 MB 对 7.1 MB）。功能测试用它没问题，但**手感会明显变差**——最小化这种时序和动画敏感的动作尤其明显。2026-07-26 就吃过一次：感觉最小化手感不对，以为是新版本引入的回归，想退回旧版本；查下来 v0.6.5、v0.6.6 和当前主线的最小化代码**是同一份**，退回去根本不会有变化，真正的差别是手上那个包是 Debug 编的。换成 Release 包，手感立刻回来。
→ 拿手感说事之前，先确认手上的包是 Release 还是 Debug；日用一律换成 Release 包。（2026-07-26 吃过）

**复制应用包不能用命令行的普通复制。**
普通复制不保留应用包里的符号链接，装完签名校验直接失败，应用可能起不来或权限失效。
→ 用访达拖拽是安全的；命令行必须用 `ditto`。

**「系统设置里开关是开的」完全不能证明权限生效。**
2026-07-29 折腾了很久：辅助功能列表里钨极的开关明明开着，应用就是拿不到权限；关开一次没用、删掉重加也没用（而且应用还在运行时减号是灰的，必须先退出才能删）。真相是**权限记录会随签名变化不断累积**——本机清理时一口气清出了 **32 条**，你看到那条开着的，属于其中早已失效的某一条。
→ 判断权限有没有真生效，**看应用是不是处于无程序坞图标的模式**（有权限才会切过去），别看开关。彻底修法是把该应用的权限记录整个清空再重新授权一次。（2026-07-29 吃过）

**日常包必须用固定证书签名，不能用临时签名。**（实测结论，别再试临时签名）
临时签名（ad-hoc）没有稳定身份，系统只能拿每次构建都变的指纹当身份，**每装一次新版授权就失配一次**——这就是上面那 32 条记录的由来，也是所有用户升级后权限失效的原因。换成固定证书后实测：指纹从 `c043dc7a` 变到 `2e84976f`、构建号 9→10，**授权依然有效**。
→ 本机日常包一律用 `Scripts/install_local_release.sh` 装（Release + 固定证书 + 装进「应用程序」），授权一次即可长期有效。（2026-07-29 实测确认）

**两个包的辅助功能授权是各自独立的，别以为授权过一次就到处都算数。**
开发构建和 `/Applications` 里的正式包，路径不同、签名也不同，**在系统眼里是两个应用**。给其中一个授权不会覆盖另一个。2026-07-29 就卡在这儿：换完正式包后应用起不来，一时分不清是"权限没给"还是"给错了应用"。
→ 遇到权限问题先确认**现在跑的是哪个包**（菜单版本行会标：带「· 开发版」的是开发构建，不带后缀的是 `/Applications` 正式包），再去看那个包的授权。（2026-07-29 吃过）

**自签名证书只要固定住，辅助功能授权就不会丢。**（正面经验，别忘）
以前每次升级授权就失效——因为签名身份每次构建都在变，系统当成了新应用。现在预览包锁定同一张证书，授权跨版本延续，升级不用重新授权。2026-07-20 实测验证。

**连着重装很多次，会把辅助功能授权弄成一条"看着是开的"死条目。**
为了做 A/B 对照，一天内重装了 5 次、反复启停 20 多次，之后应用起来了却进不了常驻状态——任务条不工作。而系统设置里那个开关**看着还是勾着的**，系统记录里那条授权也还写着"已允许"。它已经和新装的包对不上了。
→ 连续重装做对照之后，如果应用起来了却不干活、又不报错，**先怀疑授权，别去查代码**。修法：完全退出应用（运行中系统设置里的「−」是禁用的）→ 系统设置删掉那条 → 从 `/Applications` 启动 → 重新勾选。（2026-07-31 吃过）

## 验收方式

**验收前先确认只有一个实例在跑。**
上一轮的旧实例常驻后台不会自己退出，新包起来后屏幕上会同时有两个任务条，长得一模一样。这时候你测到的是哪个版本，谁也说不清——2026-07-21 就因此把一个其实没验证过的修复判成"不行"并整个回退，白折腾一轮。
→ 起新包前先扫一遍残留进程，关干净再起；报"不行"之前先确认屏幕上只有一个任务条。（2026-07-21 吃过）

**跑了很久的实例，不能拿来当性能基线。**
同一份代码，挂了几个小时的实例空闲 CPU 是 4.40%，刚起来 90 秒的是 2.60%。第一次对比拿"跑了几小时的旧版"去比"刚装好的新版"，算出降了 68%——**高估**；两组都用刚起来 90 秒的状态重测一遍，真实是 46%。连带那个"内存从 50M 降到 28M"的改进也是假的，配对后两组都是 28M。
→ 性能前后对比，两组必须同预热时长、同一天、同一台机器同样负载；跑了很久的实例只能自己跟自己比。（2026-07-31 吃过）

**"窗口没变"证明不了"没人碰电脑"。**
接上条。2026-08-03 重做这个配对测量时，脚本每一臂首尾各拍一次窗口清单来自检，结果长时臂被判作废（微信开了一个窗），新实例臂判"干净"——但它那份调用树里有 `MenuHostNSView.rightMouseDown` **2826 个采样**，也就是采样期间任务条上一直开着右键菜单，另外还有应用切换 1302、点击动作 586。窗口数抓不到菜单、悬停、点击、切应用，而这些恰恰是空闲测量最怕的东西。
→ 空闲性能采样要用**系统输入空闲时长**当硬门槛（`ioreg -c IOHIDSystem` 的 `HIDIdleTime`，单位纳秒），要求每一臂结束时空闲时长 ≥ 该臂时长；再用 `caffeinate -i` 包住全程，否则中途系统休眠会让 CPU% 算出一个假的低值。（2026-08-03 吃过）

**自检判了"作废"，脚本就必须停下——尤其当这一臂不可再生时。**
同一次测量：A 臂（跑了 8 小时的实例）已经自我判定 CONTAMINATED，脚本却照原计划往下走，退出旧实例、起了新实例。而"跑了 8 小时"这个状态**当天再也造不出来**——唯一的长时臂就这么没了，整轮测量作废且无法当天重跑。
→ 写多阶段测量脚本时，**每一阶段的自检结果必须能中止后续阶段**；判断哪一步是"不可再生资源"（长时实例、一次性现场、马上要被覆盖的状态），在它被销毁前设一道闸。（2026-08-03 吃过）

**看内存别用 `ps` 的 RSS，要用 physical footprint。**
同一次测量里，RSS 说新实例 110 MB、8 小时实例 80 MB（长时更省），physical footprint 说新实例 38.8 MB、8 小时实例 89.0 MB（长时 2.3 倍）——**两个口径给出相反的方向**。RSS 把 dyld 共享缓存等只读共享页也算进去，新进程这部分常驻得多；footprint 才是应用自己的脏内存，也是活动监视器「内存」列显示的那个数。`sample <pid>` 的报告头就直接打印 `Physical footprint`，不用另找工具。
→ 内存对比一律用 footprint；历史笔记里出现的 RSS 数字不能和 footprint 数字放在一张表里比。（2026-08-03 吃过）

**做 GUI 验收别借 owner 正在用的窗口——要自己新建一次性窗口。**
2026-08-03 验收「避让开关」需要一个铺满的窗口，图省事直接拿了 owner 开着的文本编辑和日历窗口来改尺寸。结果：中途文本编辑那个窗口被最小化、日历被退出（是他本人在用电脑，还是我的操作引起的，事后**分不清也没法证明**），目标换了两次，最后日历的原始尺寸再也还不回去了——它已经不在了，下次打开会记着我撑大过的尺寸。这是上一条"真人操作污染测量"的反面：那次是真人弄脏了我的数据，这次是我弄脏了真人的现场，而且**不可逆**。
→ 需要窗口做验收时，`open -a TextEdit <临时文件>` 起一个自己的窗口，测完关掉、删文件；别碰任何已经开着的窗口。真要借，先把原始 frame 记下来，并且**做好还不回去的准备**。（2026-08-03 吃过）

**只启动一次，验收不了启动期的问题。**
有个约五分之一概率的启动崩溃，在代码里躺了很久都没被发现——因为日常用法是开机启动一次、然后一挂几小时，撞上了也只当是"这次没起来，再点一下"。它最后是做别的实验时顺带撞出来的，不是测出来的。
→ 凡是改动**启动流程、面板创建、窗口布局**的，验收要**连续启动十几次**，别只启动一次就算过。判定"这次起来了"要看应用有没有真进入常驻状态，光看进程还在不算。（2026-07-31 吃过）

**程序模拟的点击，验证不了真实交互。**
合成的点击不携带真实交互凭证，系统对它和真人点击的处理不同——好几个方案在模拟测试里被误判成"无效"，真人一点其实是好的（反之亦然）。
→ 凡涉及焦点、点击、权限的修复，**最终必须真人点一遍**才算验收通过。

**合成鼠标移动验证不了 WindowServer 的物理边缘手势。**
2026-08-08 测「全屏触底只唤醒钨极」时，session `CGEventTap` 已经能把合成事件从物理底边夹回 1pt，control / filter 自动化各跑 20 次都没有出现系统 Dock，一度看起来方案可能成立。真实物理 A/B 给出相反结果：filter 全程 enabled、应用层光标也确实被夹回，但 Dock 仍 `20/20` 出现——WindowServer 在 session tap 之前已经消费了原始 HID 边缘信号。
→ 涉及 Dock、热角、Mission Control 等**系统级边缘手势**时，合成事件只能验证我们自己的后半段逻辑，不能验证系统是否会响应。必须先让真实物理 control 稳定复现系统行为，再解释 filter 的结果；control 不复现时，filter 的零次命中没有证明力。

**验证用完把旧版起回来，等于把修复藏起来了。**
修完一个 bug，用开发构建量过、确认好了，收尾时为了"还原 owner 的环境"把 `/Applications` 里的旧版重新起了回来，报告里只在末尾提了一句。owner 看到的当然还是原样，回一句"还是这样呀"，整轮重来。
→ 修复类任务验证通过后，二选一：**要么直接装进 owner 日常那份**（`Scripts/install_local_release.sh`），**要么在报告第一句就写明"你现在跑的这份还没有修复"**——别把这句埋在末尾。（2026-08-01 吃过）

**`pkill` 后立刻 `open` 不等于重启成功。**
2026-08-09 切换全屏 intent 设置做 A/B 时，脚本发完 `pkill` 立刻 `open`，随后只用 `pgrep` 判断“有进程”。`pkill` 只发信号、不等待退出；LaunchServices 可能仍把 `open` 交给正在终止的旧实例，而 `pgrep` 又正好看见旧 PID，于是整轮 TextEdit `3/3 + 3/3` 在没有任何 `pending` 日志的进程上完成，被误判为代码回归。等待旧 PID 完全消失、再启动并确认新 PID 不同后，同一 Release 是 `0/3 + 0/3`。
→ 任何依赖启动配置/环境变量/权限恢复的验收都必须记录 old PID，等待它消失，再 `open` 并要求 new PID 非空且不同；只看“现在有一个同名进程”没有证明重启发生过。

**"看着没变"先查跑的是哪个包，再查代码。**
接上条。判据是现成的、一秒钟的事：`ps -Ao pid,comm | grep macos-dock-cc` 看进程路径是 `/Applications` 还是 `build/`，再比 `/Applications/Tungsten Edge.app/Contents/MacOS/macos-dock-cc-v2` 的修改时间和你改代码的时间——**可执行文件比代码旧，就不可能包含修复**，不用再看别的。这次正是靠这两行在一分钟内定位的（可执行文件 22:05，代码改动 22:27）。
→ owner 报"改了没效果"时，**先验包、再验码**。（2026-08-01 吃过）

**别用 `defaults write` + 重启来切设置档位——会跟 owner 抢方向盘，而且他的选择会被悄悄覆盖。**
2026-08-02 验收「悬停效果两档」时，为了在两档之间来回切，用的是 `defaults write com.tungsten.edge.hoverStyle …` 加重启应用。问题有两层：① 应用在跑的时候，它内存里的值才是权威，外部写入会被下一次应用自己的写入盖掉，`defaults read` 和 `plutil -p` 甚至会读出互相矛盾的值，白白怀疑了一轮代码；② 更糟的是，**owner 当时正在实机试这个新功能**——他从菜单选的档位，被 agent 这一路"写值 + 重启"抹掉了，收尾时才发现设置回到了默认档。
→ 需要在两个档位之间对比时，**先确认 owner 此刻没在用**（问一句，或看时间），并且**收尾必须报告最终落在哪一档**、哪些是 agent 写的、哪些可能覆盖了 owner 的选择。读设置一律以 `plutil -p ~/Library/Preferences/<bundleid>.plist` 为准，别信应用运行期间的 `defaults read`。（2026-08-02 吃过）

**用合成事件验"悬停"是可行的，但一步到位地移过去不触发——要补一次原地微动。**
接上条。`CGWarpMouseCursorPosition` 不产生事件，`NSTrackingArea` 完全不响应；换成 `CGEvent(mouseEventSource:mouseType:.mouseMoved:…).post(tap: .cghidEventTap)` 分步移过去，光标确实到位了（`screencapture -C` 可以拍到），**但悬停态仍然没亮**；在目标点上再补两次几像素的微动，悬停立刻生效。
→ 这类验证要成立还得有**控制组**：同一批坐标在"效果开着"的档位下跑一遍，差异非零才证明指针真的命中了——否则"零变化"既可能是功能生效，也可能是压根没碰到格子。另注意任务条是半透明的，**背景窗口一变，条上的像素就跟着变**（实测 max=51 的整条差异全部来自背景），所以前后两张图之间不能让背后的窗口动。（2026-08-02 建立）

**菜单/弹层这类 GUI 改动，可以用合成右键 + 窗口列表量化验收——但它会在 owner 屏幕上真的弹菜单。**
2026-08-03 做右键任务条弹菜单时，靠这套把三件"感觉问题"全变成了数字：合成 `rightMouseDown/Up` → 轮询 `CGWindowListCopyWindowInfo` 里 `layer > 3` 的窗口判断菜单何时出现（量出右键→菜单 158–176ms，而右键 chip 的对照组只要 60–78ms，差出来的 100ms 定位到 `menuWillOpen` 里两个同步 I/O）；读菜单窗口矩形算锚点（量出菜单底边落在鼠标下方 28pt、把整条任务条盖住）；`screencapture -R` 截图后逐像素取色验证颜色改动是否真的生效（`withAlphaComponent` 作用在动态强调色上到底生不生效，靠肉眼看两张图是分不出来的）。菜单菜单窗口和 chip 菜单可以靠**尺寸**区分，不必猜坐标。
代价是每探一次就在 owner 屏幕上弹一次菜单——这一轮因此被打断了两次，第二次他直接叫停。
→ 这套方法**值得用**，但：每次探针**必须以 Esc 收尾并回读确认没有残留菜单**；一次跑完所有点位、别分很多轮；开工前看一眼 owner 是不是正在操作（他刚发过截图/刚回过消息就是在用）；需要很多次扫描时（比如逐点扫描命中区域）先问一句，别默认可以扫十几次。（2026-08-03 建立，同日被叫停两次）

**开发构建的权限归属可能和装机版不一样，于是"开发时真机验收通过"的功能，装到 `/Applications` 后可能静默失效。**（**猜测，未验证**）
2026-08-02 查「最小化时飞书主窗口被带出来」时撞上的矛盾：那段选交接目标的逻辑依赖 CG 窗口标题，而读 CG 标题需要「屏幕录制」权限；系统 TCC 表（`/Library/Application Support/com.apple.TCC/TCC.db` 的 `kTCCServiceScreenCapture`）里只有旧的 `com.caye.macosdockcc`，**没有** `com.caye.macosdockcc.v2`。按此推断装机版读不到标题、那段逻辑等于空转。但 `Docs/22` §3.5.6 白纸黑字记着当年 owner 真机实测「A2 不冒头，B 回来很自然」——说明它当时确实选到了正常目标。
一个说得通的解释是：开发构建从终端启动时，TCC 把权限归属判给了终端（Ghostty 有屏幕录制权限），装机版没有这个"靠山"。**这个解释没有验证过，别当事实用。**
→ 凡是功能依赖某个**运行时前提**（权限、系统能力、外部服务），验收时不能只看"行为看起来对了"，要**直接观察它选中了谁 / 读到了什么**；并且至少在装机版上复验一次，不能只在开发构建上验收。
→ 验证本条的方法（10 秒）：状态栏菜单 →「导出任务条快照」，看 `$TMPDIR/macos-dock-cc-v2-debug-snapshot-latest.json` 里 `source: "cg"` 的条目带不带 `title`。

## 排查方式

**动画和交互类的毛病，光读代码推不出根因；现象的细节才是决定性证据。**
2026-08-01 报「点图标启动应用，只弹跳两下就停」。读代码推出来的嫌疑犯是"判断应用起来没有"那段逻辑提前放行——听着完全说得通，一整轮探索加一整轮方案都建在这个前提上，全白做。真正的根因是补充的那句话给出的：「**鼠标从图标上移走就停，而且停在弹跳的最高点**，有时还变成斜着乱跳」。停在最高点意味着动画是被中途顶掉、不是正常收尾；和鼠标移开严格同步意味着凶手是悬停动画。这两点从代码里推不出来，只能靠人眼看到。
→ 报交互 / 动画类问题时，除了"什么坏了"，尽量多给三样：**什么时候停（做了什么动作之后）、停在什么状态（半路还是收尾）、当时鼠标在哪**。一句话能省一整轮。反过来，接到这类问题**先问细节再动代码**，别急着从代码推因。（2026-08-01 吃过）

**把"猜测"写进长期文档，下一轮就会被当成"已知事实"用。**
上一轮收尾时，两个还没查的 bug 被顺手登进回滚账本，各带一句当时的推断：剪映「疑似被误判成已在运行」、系统设置「大概率和 Apple『信息』同族，都是 Catalyst 应用」。两句都写得像结论，没标明是猜的。下一轮真去修的时候，探索直接从这两句出发——查了一圈"哪里把它误判成已在运行"，又去比对 Catalyst 应用的窗口特征，**整轮都建在错误前提上**。实测下来两句全错：剪映走的本来就是启动分支，真凶是系统按包名让已运行的后台助手顶替了主体；系统设置压根不是 Catalyst 应用，窗口一切正常，唯一异常是标题是空的。
→ 写进任何长期文档（账本、决策、看板）的**因果判断**，必须标明依据强度：实测过的写"实测"，没查的写"**猜测，未验证**"。宁可写得难看，也别让下一轮把它当地基。反过来，接手时看到没标依据的因果句，**先当假设核实一遍再用**。（2026-08-01 吃过）

**症状出现在多条不共享代码的路径上时，先找共同点，别接着深挖唯一那条能测的线。**
2026-08-04 报「菜单里鼠标划过选项不跟手」。当时唯一能测到的异常是打开菜单时主线程被约 100ms 的同步系统读取卡住，于是**连修三轮**都在修它——挪到显示后、再彻底挪到后台队列——每一轮都汇报"应该好了"，每一轮 owner 都说还是粘。真正掉头的触发点是他补的一句：「**右键应用图标出现的菜单也一样**」。那个菜单是现搭的 `NSMenu`，没有滑块、没有自绘控件、不读任何系统状态，和状态栏菜单没有一行共同代码。三个菜单唯一的共同点是"由钨极弹出"，顺着这条线一步就找到了真凶（全局鼠标监视器＝事件拦截器，我们自己弹菜单时它不被服务）。
→ 一个症状同时出现在**多条互不共享代码的路径**上，就说明根因在这些路径的**公共层**（同一个进程、同一个 run loop、同一套系统资源），不在任何一条路径的内部实现里。此时"深挖那条唯一能测到异常的线"是最贵的错误路线——它一定能挖出点什么，但挖出来的多半不是病因。**先列共同点，再选线**。（2026-08-04 吃过）

**「主线程闲着」不等于「我们没问题」——事件送得晚一样是卡。**
同一轮里，`sample` 显示菜单开着时主线程 93% 阻塞在 `mach_msg2_trap` 干等事件、我们自己的代码**零个采样**。这份数据当时被读成"我们是无辜的，问题在别处"，而它的真实含义是"**事件来得晚**"：主线程随时能干活，只是没东西给它干——因为鼠标事件正卡在我们自己装的事件拦截器里等超时。
→ 性能采样只能回答"主线程忙不忙"，回答不了"事件到没到"。看到"主线程闲着但用户说卡"，**不要据此排除自己**，要转去查事件投递路径：本进程装了哪些事件拦截器（`addGlobalMonitorForEvents` / `CGEventTap`）、它们的处理入口在当前 run loop 模式下会不会被服务。（2026-08-04 吃过）

**让 owner 在他自己机器上跑实验时，一次只给一条命令、一个观察点、一个二选一的结果。**
2026-08-05 查「全屏下能不能压住系统 Dock」。要验证的命题很单一：Dock 改了偏好之后，不重启能不能读到新值。我却一口气给了三步——先跑 `notifyutil` 发通知、看 Dock 恢不恢复正常、不行再 `killall Dock`。owner 实际是用钨极自己菜单里的系统 Dock 滑杆把 Dock 恢复的：那条路会写系统偏好**并重启 Dock**，于是"到底是通知起了作用还是重启起了作用"直接不可归因，**整个实验作废**，得重设条件从头跑一遍。更糟的是那一下顺手把他自己的「不唤醒」档改成了 0.1 秒，我是隔了好几轮偶然去读一次偏好才发现的——中间他桌面上的 Dock 一直在被误唤醒。
→ 实机实验的指令要写成**一条命令 + 一个观察点 + 一个二选一的结论**，明确写出"看到 X 说明通了，看到 Y 说明废了"。不要给「如果 A 就 B，否则 C」的链条：owner 会用自己顺手的方式达成目的，而不同路径的副作用不一样，结果就没法归因。另外**不要让他用产品自身的设置界面去做系统级实验**——那条路会同时改动他的真实配置，既污染实验也污染他的机器。实验结束后主动核一遍被碰过的键有没有回到原值，别等他发现。（2026-08-05 吃过）
